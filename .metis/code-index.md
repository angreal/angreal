# Code Index

> Generated: 2026-03-18T13:21:30Z | 69 files | Python, Rust

## Project Structure

```
├── crates/
│   └── angreal/
│       ├── src/
│       │   ├── builder/
│       │   │   ├── command_tree.rs
│       │   │   ├── mod.rs
│       │   │   └── tree_output.rs
│       │   ├── completion/
│       │   │   ├── bash.rs
│       │   │   ├── mod.rs
│       │   │   ├── templates.rs
│       │   │   └── zsh.rs
│       │   ├── error_formatter.rs
│       │   ├── git/
│       │   │   └── mod.rs
│       │   ├── init.rs
│       │   ├── integrations/
│       │   │   ├── docker_compose.rs
│       │   │   ├── flox/
│       │   │   │   └── mod.rs
│       │   │   ├── git.rs
│       │   │   ├── mod.rs
│       │   │   └── uv/
│       │   │       └── mod.rs
│       │   ├── lib.rs
│       │   ├── logger.rs
│       │   ├── macros.rs
│       │   ├── mcp.rs
│       │   ├── py_logger.rs
│       │   ├── python_bindings/
│       │   │   ├── decorators.rs
│       │   │   ├── integrations/
│       │   │   │   ├── compose.rs
│       │   │   │   ├── docker.rs
│       │   │   │   ├── flox.rs
│       │   │   │   ├── git.rs
│       │   │   │   ├── mod.rs
│       │   │   │   └── venv.rs
│       │   │   ├── mod.rs
│       │   │   ├── utils.rs
│       │   │   └── venv.rs
│       │   ├── task.rs
│       │   ├── utils.rs
│       │   └── validation/
│       │       ├── allowed_values.rs
│       │       ├── length.rs
│       │       ├── max.rs
│       │       ├── min.rs
│       │       ├── mod.rs
│       │       ├── not_empty.rs
│       │       ├── regex_match.rs
│       │       └── type_check.rs
│       └── tests/
│           ├── common/
│           │   ├── mod.rs
│           │   └── test_assets/
│           │       ├── bad_import_init.py
│           │       ├── bad_import_task.py
│           │       ├── exception_init.py
│           │       ├── exception_task.py
│           │       ├── good_init.py
│           │       ├── good_task.py
│           │       ├── internal_module/
│           │       │   └── __init__.py
│           │       ├── no_func_init.py
│           │       └── no_func_task.py
│           └── integration/
│               ├── git/
│               │   ├── mod.rs
│               │   └── utils.rs
│               ├── init.rs
│               ├── lib.rs
│               └── utils.rs
└── py_tests/
    ├── __init__.py
    ├── integrations/
    │   ├── test_docker.py
    │   ├── test_docker_compose.py
    │   ├── test_flox.py
    │   ├── test_flox_extended.py
    │   ├── test_git.py
    │   ├── test_venv.py
    │   └── test_venv_extended.py
    ├── test_angreal.py
    ├── test_entrypoint_registration.py
    ├── test_exit_codes.py
    ├── test_functional.py
    ├── test_get_context.py
    └── test_git_rust_integration.py
```

## Modules

### crates/angreal/src/builder

> *Semantic summary to be generated by AI agent.*

#### crates/angreal/src/builder/command_tree.rs

- pub `CommandNode` struct L7-19 — `{ name: String, command: Option<SerializableCommand>, about: Option<String>, chi...` — Represents a node in the command tree
- pub `SerializableCommand` struct L23-33 — `{ name: String, about: Option<String>, long_about: Option<String>, group: Option...` — Serializable version of AngrealCommand for JSON output
- pub `SerializableToolDescription` struct L37-40 — `{ description: String, risk_level: String }` — Serializable version of ToolDescription for JSON output
- pub `ProjectSchema` struct L44-48 — `{ angreal_root: String, angreal_version: String, commands: Vec<CommandSchema> }` — Tree schema for AI agent consumption
- pub `CommandSchema` struct L51-58 — `{ command: String, description: String, tool: Option<SerializableToolDescription...`
- pub `ParameterSchema` struct L61-70 — `{ name: String, flag: Option<String>, param_type: String, required: bool, descri...`
- pub `new_group` function L74-81 — `(name: String, about: Option<String>) -> Self` — Create a new group node
- pub `new_command` function L84-105 — `(name: String, command: AngrealCommand) -> Self` — Create a new command node
- pub `add_command` function L108-135 — `(&mut self, command: AngrealCommand)` — Add a command to this node or its children
- pub `to_project_schema` function L138-151 — `( &self, angreal_root: String, angreal_version: String, ) -> ProjectSchema` — Convert to new schema format
- pub `to_json` function L184-186 — `(&self) -> Result<String, serde_json::Error>` — Convert to JSON format (legacy)
- pub `to_schema_json` function L189-196 — `( &self, angreal_root: String, angreal_version: String, ) -> Result<String, serd...` — Convert to new schema JSON format
-  `CommandNode` type L72-197 — `= CommandNode`
-  `collect_commands` function L154-181 — `(&self, commands: &mut Vec<CommandSchema>, path_segments: Vec<String>)` — Recursively collect all commands from the tree
-  `tests` module L200-342 — `-`
-  `test_new_group` function L206-215 — `()`
-  `test_new_command` function L218-241 — `()`
-  `test_add_command_top_level` function L244-268 — `()`
-  `test_add_command_nested` function L271-311 — `()`
-  `test_to_project_schema` function L314-341 — `()`

#### crates/angreal/src/builder/mod.rs

- pub `command_tree` module L3 — `-`
- pub `tree_output` module L4 — `-` — The angreal app builder
- pub `select_args` function L12-19 — `(command_path: &str) -> Vec<AngrealArg>` — Get the args for a given command using full command path.
- pub `build_app` function L241-258 — `(in_angreal_project: bool) -> App<'static>` — Build the final CLI from the registered tasks
-  `base_app_setup` function L21-33 — `() -> App<'static>` — The angreal app builder
-  `add_init_subcommand` function L35-63 — `(app: App<'static>) -> App<'static>` — The angreal app builder
-  `add_completion_subcommands` function L65-86 — `(app: App<'static>) -> App<'static>` — The angreal app builder
-  `add_alias_subcommand` function L88-116 — `(app: App<'static>) -> App<'static>` — The angreal app builder
-  `add_completion_subcommand` function L118-144 — `(app: App<'static>) -> App<'static>` — The angreal app builder
-  `add_tree_subcommand` function L146-158 — `(app: App<'static>) -> App<'static>` — The angreal app builder
-  `add_mcp_subcommand` function L160-166 — `(app: App<'static>) -> App<'static>` — The angreal app builder
-  `add_project_subcommands` function L168-238 — `(mut app: App<'static>) -> App<'static>` — The angreal app builder
-  `build_clap_command` function L178-230 — `(node: &CommandNode) -> Command<'static>` — The angreal app builder
-  `tests` module L261-275 — `-` — The angreal app builder
-  `test_generate_app_in_project` function L265-268 — `()` — The angreal app builder
-  `test_generate_app_out_project` function L271-274 — `()` — The angreal app builder

#### crates/angreal/src/builder/tree_output.rs

- pub `format_arg_signature_pub` function L11-13 — `(args: &[AngrealArg]) -> String` — Format argument signature for display: [--flag] [--option=<type>]
- pub `print_tree` function L50-52 — `(node: &CommandNode, long: bool)` — Print the command tree in human-readable format
-  `format_arg_signature` function L15-34 — `(args: &[AngrealArg]) -> String` — - Long: includes full ToolDescription prose
-  `get_command_path` function L37-43 — `(node: &CommandNode, parent_path: &[String]) -> String` — Get the command path for looking up arguments
-  `print_node` function L54-126 — `(node: &CommandNode, long: bool, depth: usize, parent_path: &[String])` — - Long: includes full ToolDescription prose
-  `tests` module L129-230 — `-` — - Long: includes full ToolDescription prose
-  `test_format_arg_signature_flag` function L133-156 — `()` — - Long: includes full ToolDescription prose
-  `test_format_arg_signature_value` function L159-182 — `()` — - Long: includes full ToolDescription prose
-  `test_format_arg_signature_multiple` function L185-229 — `()` — - Long: includes full ToolDescription prose

### crates/angreal/src/completion

> *Semantic summary to be generated by AI agent.*

#### crates/angreal/src/completion/bash.rs

- pub `generate_completion_script` function L4-63 — `() -> String` — Generate bash completion script for Angreal
-  `tests` module L66-76 — `-` — Bash completion script generation for Angreal
-  `test_generate_completion_script` function L70-75 — `()` — Bash completion script generation for Angreal

#### crates/angreal/src/completion/mod.rs

- pub `bash` module L14 — `-` — - Automatic setup on first run
- pub `templates` module L15 — `-` — - Automatic setup on first run
- pub `zsh` module L16 — `-` — - Automatic setup on first run
- pub `Shell` enum L20-24 — `Bash | Zsh | Unknown` — Supported shells for completion
- pub `detect` function L28-52 — `() -> Self` — Detect the current shell from environment
- pub `name` function L55-61 — `(&self) -> &str` — Get the name of this shell
- pub `CompletionConfig` struct L65-69 — `{ shell: Shell, install_path: PathBuf, completion_script: String }` — Configuration for shell completion
- pub `for_current_shell` function L73-102 — `() -> Result<Self>` — Create completion config for detected shell
- pub `is_installed` function L105-107 — `(&self) -> bool` — Check if completion is already installed
- pub `install` function L110-129 — `(&self) -> Result<()>` — Install completion script
- pub `should_auto_install` function L235-247 — `() -> bool` — Check if completion should be auto-installed
- pub `auto_install_completion` function L250-275 — `() -> Result<()>` — Auto-install completion if appropriate
- pub `force_install_completion` function L278-339 — `(shell: Option<&str>) -> Result<()>` — Force install completion for specific shell or detected shell
- pub `uninstall_completion` function L342-405 — `(shell: Option<&str>) -> Result<()>` — Uninstall completion for specific shell or detected shell
- pub `show_completion_status` function L408-461 — `() -> Result<()>` — Show completion installation status
- pub `generate_completions` function L464-545 — `(args: &[String]) -> Result<Vec<String>>` — Generate completions for current command line
-  `Shell` type L26-62 — `= Shell` — - Automatic setup on first run
-  `CompletionConfig` type L71-196 — `= CompletionConfig` — - Automatic setup on first run
-  `ensure_sourced` function L132-195 — `(&self) -> Result<()>` — Ensure completion script is sourced in shell rc file
-  `find_zsh_completion_dir` function L199-232 — `(home: &str) -> Result<PathBuf>` — Find zsh completion directory
-  `get_available_tasks` function L548-581 — `() -> Result<Vec<String>>` — Get available tasks in current project
-  `get_nested_command_completions` function L584-629 — `(args: &[String]) -> Result<Vec<String>>` — Get completions for nested commands
-  `tests` module L632-649 — `-` — - Automatic setup on first run
-  `test_shell_detection` function L636-642 — `()` — - Automatic setup on first run
-  `test_should_auto_install` function L645-648 — `()` — - Automatic setup on first run

#### crates/angreal/src/completion/templates.rs

- pub `get_template_suggestions` function L28-46 — `() -> Result<Vec<String>>` — Get template suggestions for completion
-  `GitHubRepo` struct L18-25 — `{ name: String, description: Option<String>, url: String }` — GitHub repository information
-  `get_local_templates` function L49-77 — `() -> Result<Vec<String>>` — Get locally cached templates from ~/.angrealrc/
-  `get_github_templates` function L80-114 — `() -> Result<Vec<String>>` — Get template repositories from GitHub angreal organization
-  `is_template_repo` function L117-122 — `(repo: &GitHubRepo) -> bool` — Determine if a GitHub repository is a template (exclude meta repos)
-  `tests` module L125-158 — `-` — - GitHub angreal organization repositories
-  `test_get_local_templates` function L129-132 — `()` — - GitHub angreal organization repositories
-  `test_is_template_repo` function L135-149 — `()` — - GitHub angreal organization repositories
-  `test_get_template_suggestions` function L152-157 — `()` — - GitHub angreal organization repositories

#### crates/angreal/src/completion/zsh.rs

- pub `generate_completion_script` function L4-37 — `() -> String` — Generate zsh completion script for Angreal
-  `tests` module L40-50 — `-` — Zsh completion script generation for Angreal
-  `test_generate_completion_script` function L44-49 — `()` — Zsh completion script generation for Angreal

### crates/angreal/src

> *Semantic summary to be generated by AI agent.*

#### crates/angreal/src/error_formatter.rs

- pub `PythonErrorFormatter` struct L6-8 — `{ error: PyErr }` — Formats Python exception information in a more readable way
- pub `new` function L11-13 — `(error: PyErr) -> Self`
- pub `format` function L16-50 — `(&self) -> String` — Formats a Python error in a more readable way
-  `PythonErrorFormatter` type L10-51 — `= PythonErrorFormatter`
-  `PythonErrorFormatter` type L53-57 — `= PythonErrorFormatter`
-  `fmt` function L54-56 — `(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result`
-  `tests` module L60-81 — `-`
-  `test_error_formatter` function L65-80 — `()`

#### crates/angreal/src/init.rs

- pub `init` function L27-101 — `(template: &str, force: bool, take_inputs: bool, values_file: Option<&str>)` — Initialize a new project by rendering a template.
- pub `create_home_dot_angreal` function L232-241 — `() -> PathBuf` — create the angreal caching directory for storing cloned templates
- pub `render_template` function L244-296 — `( path: &Path, take_input: bool, force: bool, values_file: Option<&str>, ) -> St...` — render the provided angreal template path
-  `get_scheme` function L104-115 — `(u: &str) -> Result<String, String>` — get the schema for the provided template
-  `handle_file_template` function L117-209 — `(template: &str, angreal_home: &Path) -> String` — The angreal `init` command.
-  `handle_git_template` function L211-229 — `(template: &str, angreal_home: PathBuf) -> PathBuf` — The angreal `init` command.

#### crates/angreal/src/lib.rs

- pub `macros` module L11 — `-` — project.
- pub `builder` module L13 — `-` — project.
- pub `completion` module L14 — `-` — project.
- pub `error_formatter` module L15 — `-` — project.
- pub `git` module L16 — `-` — project.
- pub `init` module L17 — `-` — project.
- pub `integrations` module L18 — `-` — project.
- pub `logger` module L19 — `-` — project.
- pub `mcp` module L20 — `-` — project.
- pub `py_logger` module L21 — `-` — project.
- pub `python_bindings` module L22 — `-` — project.
- pub `task` module L23 — `-` — project.
- pub `utils` module L24 — `-` — project.
- pub `validation` module L25 — `-` — project.
- pub `initialize_python_tasks` function L861-961 — `() -> Result<(), Box<dyn std::error::Error>>` — Initialize Python bindings and load angreal tasks for external tools
-  `PyGit` struct L49-51 — `{ inner: Git }` — project.
-  `PyGit` type L54-158 — `= PyGit` — project.
-  `new` function L57-61 — `(working_dir: Option<&str>) -> PyResult<Self>` — project.
-  `execute` function L63-70 — `(&self, subcommand: &str, args: Vec<String>) -> PyResult<(i32, String, String)>` — project.
-  `init` function L72-76 — `(&self, bare: Option<bool>) -> PyResult<()>` — project.
-  `add` function L78-83 — `(&self, paths: Vec<String>) -> PyResult<()>` — project.
-  `commit` function L85-89 — `(&self, message: &str, all: Option<bool>) -> PyResult<()>` — project.
-  `push` function L91-95 — `(&self, remote: Option<&str>, branch: Option<&str>) -> PyResult<()>` — project.
-  `pull` function L97-101 — `(&self, remote: Option<&str>, branch: Option<&str>) -> PyResult<()>` — project.
-  `status` function L103-107 — `(&self, short: Option<bool>) -> PyResult<String>` — project.
-  `branch` function L109-113 — `(&self, name: Option<&str>, delete: Option<bool>) -> PyResult<String>` — project.
-  `checkout` function L115-119 — `(&self, branch: &str, create: Option<bool>) -> PyResult<()>` — project.
-  `tag` function L121-125 — `(&self, name: &str, message: Option<&str>) -> PyResult<()>` — project.
-  `__call__` function L127-157 — `( &self, command: &str, args: Vec<String>, kwargs: Option<&Bound<'_, pyo3::types...` — project.
-  `git_clone` function L162-166 — `(remote: &str, destination: Option<&str>) -> PyResult<String>` — project.
-  `ensure_uv_installed` function L169-172 — `() -> PyResult<()>` — project.
-  `uv_version` function L175-178 — `() -> PyResult<String>` — project.
-  `create_virtualenv` function L181-185 — `(path: &str, python_version: Option<&str>) -> PyResult<()>` — project.
-  `install_packages` function L188-194 — `(venv_path: &str, packages: Vec<String>) -> PyResult<()>` — project.
-  `install_requirements` function L197-203 — `(venv_path: &str, requirements_file: &str) -> PyResult<()>` — project.
-  `discover_pythons` function L206-215 — `() -> PyResult<Vec<(String, String)>>` — project.
-  `install_python` function L218-222 — `(version: &str) -> PyResult<String>` — project.
-  `get_venv_activation_info` function L225-231 — `(venv_path: &str) -> PyResult<integrations::uv::ActivationInfo>` — project.
-  `register_entrypoint` function L234-363 — `(name: &str) -> PyResult<()>` — project.
-  `list_entrypoints` function L366-392 — `() -> PyResult<Vec<String>>` — project.
-  `unregister_entrypoint` function L395-456 — `(name: &str) -> PyResult<()>` — project.
-  `cleanup_entrypoints` function L459-470 — `() -> PyResult<()>` — project.
-  `main` function L474-857 — `() -> PyResult<()>` — The main function is just an entry point to be called from the core angreal library.
-  `angreal` function L964-1046 — `(m: &Bound<'_, PyModule>) -> PyResult<()>` — project.
-  `_integrations` function L1049-1055 — `(m: &Bound<'_, PyModule>) -> PyResult<()>` — project.
-  `docker` function L1058-1077 — `(m: &Bound<'_, PyModule>) -> PyResult<()>` — project.
-  `git_module` function L1080-1084 — `(m: &Bound<'_, PyModule>) -> PyResult<()>` — project.

#### crates/angreal/src/logger.rs

- pub `init_logger` function L9-21 — `() -> Handle` — initializes the angreal logger instance
- pub `update_verbosity` function L24-39 — `(log_hndl: &Handle, verbosity: u8)` — updates the verbosity of the logger after initialization

#### crates/angreal/src/macros.rs

-  `pythonize_this` macro L2-6 — `-`
-  `attr_copy` macro L8-35 — `-`
-  `result_or_return_err` macro L38-45 — `-`
-  `value_or_return_err` macro L48-55 — `-`

#### crates/angreal/src/mcp.rs

- pub `serve` function L16-44 — `()` — Run the MCP stdio server.
-  `handle_message` function L46-101 — `(msg: &Value, instructions: &str) -> Option<String>` — exposes no tools — its sole purpose is context injection.
-  `build_instructions` function L103-134 — `() -> String` — exposes no tools — its sole purpose is context injection.
-  `capture_tree_output` function L137-146 — `() -> String` — Capture tree output as a string instead of printing to stdout.
-  `tree_to_string` function L149-232 — `( node: &CommandNode, long: bool, depth: usize, parent_path: &[String], buf: &mu...` — Write the tree to a buffer (mirrors tree_output::print_tree logic).

#### crates/angreal/src/py_logger.rs

- pub `register` function L6-12 — `()` — registers the rust logging interface with the python logging interface.
- pub `setup_logging` function L76-104 — `(py: Python) -> PyResult<()>` — Modifies the Python `logging` module to deliver its log messages to the host `tracing::Subscriber` by default.
-  `host_log` function L15-65 — `(record: &Bound<'_, PyAny>) -> PyResult<()>` — Consume a Python `logging.LogRecord` and emit a Rust `Log` instead.

#### crates/angreal/src/task.rs

- pub `register` function L13-21 — `(_py: Python<'_>, m: &Bound<'_, PyModule>) -> PyResult<()>` — Registers the Command and Arg structs to the python api in the `angreal` module
- pub `ANGREAL_TASKS` variable L24-25 — `: Lazy<Mutex<HashMap<String, AngrealCommand>>>` — A long lived structure that stores AngrealCommands upon registration, keyed by full path
- pub `ANGREAL_ARGS` variable L28-29 — `: Lazy<Mutex<HashMap<String, Vec<AngrealArg>>>>` — A long lived structure that stores AngrealArgs for commands upon registration, keyed by command path
- pub `ANGREAL_GROUPS` variable L32 — `: Lazy<Mutex<Vec<AngrealGroup>>>` — A long lived structure that stores Angreal Groups for commands upon registration
- pub `set_current_command_path` function L40-42 — `(path: String)` — Set the current command path for argument registration
- pub `get_current_command_path` function L45-47 — `() -> Option<String>` — Get the current command path for argument registration
- pub `generate_command_path_key` function L50-64 — `(command: &AngrealCommand) -> String` — Generate a full path key for a command based on its group hierarchy
- pub `generate_path_key_from_parts` function L67-75 — `(groups: &[String], command_name: &str) -> String` — Generate a full path key for a command based on group names and command name
- pub `AngrealGroup` struct L80-87 — `{ name: String, about: Option<String> }` — A group is just a special type of sub-command
- pub `ToolDescription` struct L128-135 — `{ description: String, risk_level: String }` — Rich description for exposing a command to AI agents
- pub `AngrealCommand` struct L199-218 — `{ name: String, about: Option<String>, long_about: Option<String>, func: Py<PyAn...` — A command describes a subcommand to be registered with the CLI
- pub `add_group` function L299-350 — `(&mut self, group: AngrealGroup) -> PyResult<()>` — Add a (task::AngrealGroup) to the task::AngrealCommand called on
- pub `AngrealArg` struct L356-407 — `{ name: String, command_name: String, command_path: String, takes_value: Option<...` — An argument to augment the behavior of an angreal command
-  `ToolDescription` type L138-168 — `= ToolDescription` — Core structures for describing tasks and arguments
-  `__new__` function L141-159 — `(description: &str, risk_level: Option<&str>) -> Self` — Core structures for describing tasks and arguments
-  `__repr__` function L161-167 — `(&self) -> String` — Core structures for describing tasks and arguments
-  `AngrealGroup` type L171-194 — `= AngrealGroup` — Methods exposed in the python API
-  `__new__` function L174-193 — `(name: &str, about: Option<&str>) -> Self` — Core structures for describing tasks and arguments
-  `AngrealCommand` type L220-231 — `impl Clone for AngrealCommand` — Core structures for describing tasks and arguments
-  `clone` function L221-230 — `(&self) -> Self` — Core structures for describing tasks and arguments
-  `AngrealCommand` type L235-351 — `= AngrealCommand` — Methods exposed to the python API
-  `__new__` function L261-297 — `( name: &str, func: Py<PyAny>, about: Option<&str>, long_about: Option<&str>, gr...` — Initialization method for the object.
-  `AngrealArg` type L410-499 — `= AngrealArg` — Core structures for describing tasks and arguments
-  `__new__` function L436-498 — `( name: &str, command_name: &str, default_value: Option<&str>, is_flag: Option<b...` — Adds an argument to an angreal command.
-  `tests` module L502-728 — `-` — Core structures for describing tasks and arguments
-  `test_hierarchical_command_registration` function L507-590 — `()` — Core structures for describing tasks and arguments
-  `test_argument_collision_resolution` function L593-711 — `()` — Core structures for describing tasks and arguments
-  `test_path_key_generation` function L714-727 — `()` — Core structures for describing tasks and arguments

#### crates/angreal/src/utils.rs

- pub `context_to_map` function L34-36 — `(ctx: Context) -> Map<String, Value>` — Filesystem utilities
- pub `repl_context_from_toml` function L40-154 — `(toml_path: PathBuf, take_input: bool) -> Context` — Filesystem utilities
- pub `render_dir` function L157-257 — `(src: &Path, context: Context, dst: &Path, force: bool) -> Vec<String>` — Filesystem utilities
- pub `check_up_to_date` function L259-292 — `() -> Result<()>` — Filesystem utilities
- pub `get_task_files` function L304-328 — `(path: PathBuf) -> Result<Vec<PathBuf>>` — Get a list of task files in given a path
- pub `register` function L331-338 — `(_py: Python<'_>, m: &Bound<'_, PyModule>) -> PyResult<()>` — Registers the Command and Arg structs to the python api in the `angreal` module
- pub `render_directory` function L341-364 — `( src: &str, dst: &str, force: bool, context: Option<&Bound<'_, PyDict>>, ) -> P...` — Filesystem utilities
- pub `is_angreal_project` function L460-491 — `() -> Result<PathBuf>` — Tests whether or not a current path is an angreal project
- pub `load_python` function L502-543 — `(file: PathBuf) -> Result<(), PyErr>` — Loads a python file as a pyo3 PyModule
- pub `extract_key_defaults` function L569-585 — `(toml_path: PathBuf) -> Result<Map<String, Value>>` — Extract key/default value pairs from a TOML file
- pub `extract_validation_rules` function L616-640 — `(toml_path: PathBuf) -> Result<Map<String, Value>>` — Extract validation rules from a TOML file
- pub `extract_prompts` function L667-679 — `(toml_path: PathBuf) -> Result<Map<String, Value>>` — Extract prompts from a TOML file
-  `generate_context` function L374-379 — `(path: &str, take_input: bool) -> PyResult<Py<PyAny>>` — Generate a templating context from a toml file.
-  `get_root` function L390-397 — `() -> PyResult<String>` — Get the root path of a current angreal project.
-  `render_template` function L400-410 — `(template: &str, context: &Bound<'_, PyDict>) -> PyResult<String>` — Filesystem utilities
-  `get_context` function L420-448 — `() -> PyResult<Py<PyAny>>` — Read the angreal.toml file from the .angreal folder and return it as a dictionary
-  `tests` module L683-1192 — `-` — Filesystem utilities
-  `common` module L692 — `-` — Filesystem utilities
-  `test_repl_context_from_toml` function L695-718 — `()` — Filesystem utilities
-  `test_load_python` function L720-748 — `()` — Filesystem utilities
-  `test_is_angreal_project` function L751-760 — `()` — Filesystem utilities
-  `test_is_not_angreal_project` function L763-773 — `()` — Filesystem utilities
-  `test_get_task_files` function L776-799 — `()` — Filesystem utilities
-  `test_extract_key_defaults` function L802-836 — `()` — Filesystem utilities
-  `test_extract_validation_rules` function L839-1010 — `()` — Filesystem utilities
-  `test_extract_prompts` function L1013-1062 — `()` — Filesystem utilities
-  `test_read_config` function L1065-1131 — `()` — Filesystem utilities
-  `test_read_config_python_bindings` function L1135-1191 — `()` — Filesystem utilities

### crates/angreal/src/git

> *Semantic summary to be generated by AI agent.*

#### crates/angreal/src/git/mod.rs

- pub `remote_exists` function L9-74 — `(remote: &str) -> bool` — Check if a remote repository exists
- pub `git_clone` function L77-94 — `(remote: &str, path: &str) -> PathBuf` — Clone a git repository to a specific location
- pub `git_clone_here` function L97-140 — `(remote: &str) -> PathBuf` — Clone a git repository to the current directory
- pub `git_pull_ff` function L143-206 — `(path: &str) -> PathBuf` — Pull changes from a remote repository

### crates/angreal/src/integrations

> *Semantic summary to be generated by AI agent.*

#### crates/angreal/src/integrations/docker_compose.rs

- pub `DockerCompose` struct L13-17 — `{ compose_file: PathBuf, working_dir: PathBuf, project_name: Option<String> }` — Docker Compose integration using subprocess execution
- pub `ComposeOutput` struct L21-26 — `{ success: bool, exit_code: i32, stdout: String, stderr: String }` — Result structure for Docker Compose operations
- pub `new` function L30-50 — `(compose_file: P) -> Result<Self>` — Create a new Docker Compose instance
- pub `with_project_name` function L53-56 — `(mut self, name: S) -> Self` — Set a custom project name
- pub `is_available` function L59-75 — `() -> bool` — Check if docker-compose is available
- pub `compose_file` function L78-80 — `(&self) -> &Path` — Get the compose file path
- pub `working_dir` function L83-85 — `(&self) -> &Path` — Get the working directory
- pub `project_name` function L88-90 — `(&self) -> Option<&str>` — Get the project name
- pub `up` function L142-167 — `(&self, options: UpOptions) -> Result<ComposeOutput>` — Start services (docker-compose up)
- pub `down` function L170-185 — `(&self, options: DownOptions) -> Result<ComposeOutput>` — Stop and remove services (docker-compose down)
- pub `restart` function L188-202 — `(&self, options: RestartOptions) -> Result<ComposeOutput>` — Restart services (docker-compose restart)
- pub `logs` function L205-229 — `(&self, options: LogsOptions) -> Result<ComposeOutput>` — View service logs (docker-compose logs)
- pub `ps` function L232-251 — `(&self, options: PsOptions) -> Result<ComposeOutput>` — List running services (docker-compose ps)
- pub `build` function L254-273 — `(&self, options: BuildOptions) -> Result<ComposeOutput>` — Build services (docker-compose build)
- pub `start` function L276-282 — `(&self, services: &[String]) -> Result<ComposeOutput>` — Start services (docker-compose start)
- pub `stop` function L285-299 — `(&self, options: StopOptions) -> Result<ComposeOutput>` — Stop services (docker-compose stop)
- pub `exec` function L302-341 — `( &self, service: &str, command: &[String], options: ExecOptions, ) -> Result<Co...` — Execute a command in a service container (docker-compose exec)
- pub `pull` function L344-350 — `(&self, services: &[String]) -> Result<ComposeOutput>` — Pull service images (docker-compose pull)
- pub `config` function L353-367 — `(&self, options: ConfigOptions) -> Result<ComposeOutput>` — Validate and view the compose configuration (docker-compose config)
- pub `UpOptions` struct L372-379 — `{ detach: bool, build: bool, remove_orphans: bool, force_recreate: bool, no_recr...` — Options for docker-compose up command
- pub `DownOptions` struct L383-387 — `{ volumes: bool, remove_orphans: bool, timeout: Option<String> }` — Options for docker-compose down command
- pub `RestartOptions` struct L391-394 — `{ timeout: Option<String>, services: Vec<String> }` — Options for docker-compose restart command
- pub `LogsOptions` struct L398-404 — `{ follow: bool, timestamps: bool, tail: Option<String>, since: Option<String>, s...` — Options for docker-compose logs command
- pub `PsOptions` struct L408-413 — `{ all: bool, quiet: bool, services: bool, filter_services: Vec<String> }` — Options for docker-compose ps command
- pub `BuildOptions` struct L417-422 — `{ no_cache: bool, pull: bool, parallel: bool, services: Vec<String> }` — Options for docker-compose build command
- pub `StopOptions` struct L426-429 — `{ timeout: Option<String>, services: Vec<String> }` — Options for docker-compose stop command
- pub `ExecOptions` struct L433-439 — `{ detach: bool, tty: bool, user: Option<String>, workdir: Option<String>, env: H...` — Options for docker-compose exec command
- pub `ConfigOptions` struct L443-447 — `{ quiet: bool, services: bool, volumes: bool }` — Options for docker-compose config command
-  `DockerCompose` type L28-137 — `= DockerCompose` — using subprocess execution to ensure compatibility with all Docker Compose versions.
-  `execute_command` function L93-136 — `(&self, args: &[&str]) -> Result<ComposeOutput>` — Execute a docker-compose command
-  `DockerCompose` type L140-368 — `= DockerCompose` — High-level Docker Compose operations
-  `tests` module L450-524 — `-` — using subprocess execution to ensure compatibility with all Docker Compose versions.
-  `test_docker_compose_available` function L456-463 — `()` — using subprocess execution to ensure compatibility with all Docker Compose versions.
-  `test_docker_compose_new` function L466-490 — `()` — using subprocess execution to ensure compatibility with all Docker Compose versions.
-  `test_docker_compose_with_project_name` function L493-517 — `()` — using subprocess execution to ensure compatibility with all Docker Compose versions.
-  `test_invalid_compose_file` function L520-523 — `()` — using subprocess execution to ensure compatibility with all Docker Compose versions.

#### crates/angreal/src/integrations/git.rs

- pub `Git` struct L8-10 — `{ working_dir: PathBuf }` — Git integration using git2/libgit2 for reliability and self-contained operation
- pub `new` function L14-27 — `(working_dir: Option<&Path>) -> Result<Self>` — Create a new Git instance
- pub `is_available` function L30-32 — `() -> bool` — Check if git2 is available (always true since it's built-in)
- pub `working_dir` function L35-37 — `(&self) -> &Path` — Get the working directory path
- pub `GitOutput` struct L67-72 — `{ success: bool, exit_code: i32, stdout: String, stderr: String }`
- pub `init` function L76-83 — `(&self, bare: bool) -> Result<()>`
- pub `clone` function L85-98 — `(remote: &str, destination: Option<&Path>) -> Result<PathBuf>`
- pub `add` function L100-115 — `(&self, paths: &[&str]) -> Result<()>`
- pub `commit` function L117-157 — `(&self, message: &str, all: bool) -> Result<()>`
- pub `push` function L159-169 — `(&self, remote: Option<&str>, branch: Option<&str>) -> Result<()>`
- pub `pull` function L171-176 — `(&self, _remote: Option<&str>, _branch: Option<&str>) -> Result<()>`
- pub `status` function L178-266 — `(&self, short: bool) -> Result<String>`
- pub `branch` function L268-301 — `(&self, name: Option<&str>, delete: bool) -> Result<String>`
- pub `checkout` function L303-322 — `(&self, branch: &str, create: bool) -> Result<()>`
- pub `remote_add` function L324-328 — `(&self, name: &str, url: &str) -> Result<()>`
- pub `remote_remove` function L330-334 — `(&self, name: &str) -> Result<()>`
- pub `tag` function L336-349 — `(&self, name: &str, message: Option<&str>) -> Result<()>`
- pub `execute` function L353-427 — `(&self, subcommand: &str, args: &[&str]) -> Result<GitOutput>` — Compatibility method for subprocess-style interface
- pub `execute_with_options` function L430-480 — `( &self, subcommand: &str, options: HashMap<&str, &str>, args: &[&str], ) -> Res...` — Compatibility method for options-based interface
-  `Git` type L12-64 — `= Git`
-  `get_repo` function L40-47 — `(&self) -> Result<Repository>` — Get or open repository
-  `get_signature` function L50-63 — `(&self) -> Result<Signature<'_>>` — Get default signature for commits
-  `Git` type L75-481 — `= Git` — High-level git operations using git2
-  `extract_repo_name` function L483-501 — `(url: &str) -> Result<String>`
-  `tests` module L504-571 — `-`
-  `test_git_available` function L509-516 — `()`
-  `test_git_init` function L519-529 — `()`
-  `test_git_operations` function L532-554 — `()`
-  `test_extract_repo_name` function L557-570 — `()`

#### crates/angreal/src/integrations/mod.rs

- pub `docker_compose` module L1 — `-`
- pub `flox` module L2 — `-`
- pub `git` module L3 — `-`
- pub `uv` module L4 — `-`

### crates/angreal/src/integrations/flox

> *Semantic summary to be generated by AI agent.*

#### crates/angreal/src/integrations/flox/mod.rs

- pub `FloxIntegration` struct L12 — `-` — Core integration for the Flox CLI
- pub `is_available` function L16-22 — `() -> bool` — Check if the `flox` binary is available in PATH
- pub `version` function L25-36 — `() -> Result<String>` — Get the Flox version string
- pub `ServiceStatus` struct L41-45 — `{ name: String, status: String, pid: Option<u32> }` — Represents a service status entry from `flox services status`
- pub `FloxEnvironment` struct L48-51 — `{ path: PathBuf }` — Flox environment wrapper for a specific directory
- pub `new` function L55-59 — `(path: P) -> Self` — Create a new FloxEnvironment reference for the given path
- pub `exists` function L62-64 — `(&self) -> bool` — Check if this directory contains a Flox environment
- pub `manifest_path` function L67-69 — `(&self) -> PathBuf` — Get the path to the manifest.toml if it exists
- pub `has_manifest` function L72-74 — `(&self) -> bool` — Check if the manifest.toml exists
- pub `get_activation_env` function L80-96 — `(&self) -> Result<HashMap<String, String>>` — Get environment variable modifications from `flox activate --print-script`
- pub `run_in_env` function L139-153 — `(&self, command: &str, args: &[&str]) -> Result<Output>` — Run a command within the Flox environment
- pub `services_start` function L159-175 — `(&self, services: &[&str]) -> Result<()>` — Start Flox services
- pub `services_stop` function L181-197 — `(&self, services: &[&str]) -> Result<()>` — Stop Flox services
- pub `services_status` function L207-231 — `(&self) -> Result<Vec<ServiceStatus>>` — Get status of all Flox services
- pub `services_logs` function L263-285 — `(&self, service: &str, follow: bool, tail: Option<u32>) -> Result<String>` — Get logs for a specific service
- pub `services_restart` function L288-304 — `(&self, services: &[&str]) -> Result<()>` — Restart services
-  `FloxIntegration` type L14-37 — `= FloxIntegration` — environment activation and services management.
-  `FloxEnvironment` type L53-305 — `= FloxEnvironment` — environment activation and services management.
-  `parse_activation_script` function L105-122 — `(script: &str) -> Result<HashMap<String, String>>` — Parse the activation script to extract environment variable exports
-  `unquote_value` function L125-134 — `(value: &str) -> String` — Remove surrounding quotes from a value
-  `parse_services_status` function L234-260 — `(output: &str) -> Result<Vec<ServiceStatus>>` — Parse the output of `flox services status`
-  `tests` module L308-484 — `-` — environment activation and services management.
-  `test_is_available_returns_bool` function L314-317 — `()` — environment activation and services management.
-  `test_version_when_flox_not_installed` function L320-331 — `()` — environment activation and services management.
-  `test_flox_environment_new` function L336-339 — `()` — environment activation and services management.
-  `test_manifest_path` function L342-348 — `()` — environment activation and services management.
-  `test_parse_activation_script_export_double_quotes` function L353-364 — `()` — environment activation and services management.
-  `test_parse_activation_script_export_single_quotes` function L367-376 — `()` — environment activation and services management.
-  `test_parse_activation_script_export_no_quotes` function L379-385 — `()` — environment activation and services management.
-  `test_parse_activation_script_empty` function L388-392 — `()` — environment activation and services management.
-  `test_parse_activation_script_ignores_non_export` function L395-405 — `()` — environment activation and services management.
-  `test_unquote_value_double_quotes` function L408-413 — `()` — environment activation and services management.
-  `test_unquote_value_single_quotes` function L416-421 — `()` — environment activation and services management.
-  `test_unquote_value_no_quotes` function L424-426 — `()` — environment activation and services management.
-  `test_unquote_value_mismatched_quotes` function L429-432 — `()` — environment activation and services management.
-  `test_parse_services_status_multiple_services` function L437-457 — `()` — environment activation and services management.
-  `test_parse_services_status_empty` function L460-464 — `()` — environment activation and services management.
-  `test_parse_services_status_header_only` function L467-471 — `()` — environment activation and services management.
-  `test_service_status_struct` function L474-483 — `()` — environment activation and services management.

### crates/angreal/src/integrations/uv

> *Semantic summary to be generated by AI agent.*

#### crates/angreal/src/integrations/uv/mod.rs

- pub `UvIntegration` struct L6 — `-`
- pub `is_available` function L9-15 — `() -> bool`
- pub `version` function L17-28 — `() -> Result<String>`
- pub `ensure_installed` function L30-69 — `() -> Result<()>`
- pub `ActivationInfo` struct L74-83 — `{ venv_path: String, venv_prefix: String, site_packages: String, python_executab...`
- pub `UvVirtualEnv` struct L85-87 — `{ path: PathBuf }`
- pub `create` function L90-110 — `(path: &Path, python_version: Option<&str>) -> Result<Self>`
- pub `install_packages` function L112-137 — `(&self, packages: &[String]) -> Result<()>`
- pub `install_requirements` function L139-158 — `(&self, requirements_file: &Path) -> Result<()>`
- pub `python_executable` function L160-166 — `(&self) -> PathBuf`
- pub `site_packages` function L168-235 — `(&self) -> Result<PathBuf>`
- pub `get_activation_info` function L237-260 — `(&self) -> Result<ActivationInfo>`
- pub `discover_pythons` function L262-293 — `() -> Result<Vec<(String, PathBuf)>>`
- pub `install_python` function L295-329 — `(version: &str) -> Result<PathBuf>`
-  `UvIntegration` type L8-70 — `= UvIntegration`
-  `UvVirtualEnv` type L89-330 — `= UvVirtualEnv`
-  `tests` module L333-611 — `-`
-  `test_uv_is_available` function L341-345 — `()`
-  `test_uv_version` function L348-358 — `()`
-  `test_uv_ensure_installed` function L361-365 — `()`
-  `test_create_venv` function L370-383 — `()`
-  `test_python_executable_path` function L386-411 — `()`
-  `test_site_packages` function L418-437 — `()`
-  `test_install_packages` function L444-470 — `()`
-  `test_install_empty_packages` function L473-482 — `()`
-  `test_install_requirements_file` function L485-501 — `()`
-  `test_get_activation_info` function L504-533 — `()`
-  `test_discover_pythons` function L536-555 — `()`
-  `test_create_venv_with_python_version` function L558-567 — `()`
-  `test_venv_struct_path` function L570-578 — `()`
-  `test_install_invalid_package` function L581-594 — `()`
-  `test_install_requirements_nonexistent_file` function L597-610 — `()`

### crates/angreal/src/python_bindings

> *Semantic summary to be generated by AI agent.*

#### crates/angreal/src/python_bindings/decorators.rs

- pub `required_version` function L21-52 — `(specifier: &str) -> PyResult<()>` — Check if the current angreal version meets the specified requirement
- pub `register_decorators` function L55-65 — `(_py: Python, m: &Bound<'_, PyModule>) -> PyResult<()>` — Register decorator functions to a Python module
- pub `GroupDecorator` struct L70-73 — `{ name: String, about: Option<String> }` — A Python callable that wraps the group decorator logic
- pub `group` function L118-132 — `(kwargs: Option<&Bound<'_, PyDict>>) -> PyResult<GroupDecorator>` — Create a group decorator that assigns commands to a group
- pub `command_group` function L141-146 — `(name: &str, about: Option<&str>) -> PyResult<GroupDecorator>` — Generate a reusable command group decorator
- pub `CommandDecorator` struct L150-155 — `{ name: Option<String>, about: Option<String>, long_about: Option<String>, tool:...` — A Python callable that wraps the command decorator logic
- pub `command` function L337-369 — `(kwargs: Option<&Bound<'_, PyDict>>) -> PyResult<CommandDecorator>` — Create a command decorator that registers functions as commands
- pub `argument` function L377-389 — `(kwargs: Option<&Bound<'_, PyDict>>) -> PyResult<ArgumentDecorator>` — Create an argument decorator that adds command-line arguments to commands
- pub `ArgumentDecorator` struct L393-397 — `{ name: String, kwargs_dict: Option<Py<PyAny>> }` — A Python callable that wraps the argument decorator logic
-  `GroupDecorator` type L76-110 — `= GroupDecorator` — - command_group helper function
-  `__call__` function L78-109 — `(&self, func: Option<Py<PyAny>>) -> PyResult<Py<PyAny>>` — - command_group helper function
-  `CommandDecorator` type L158-329 — `= CommandDecorator` — - command_group helper function
-  `__call__` function L160-328 — `(&self, func: Py<PyAny>) -> PyResult<Py<PyAny>>` — - command_group helper function
-  `ArgumentDecorator` type L399-409 — `impl Clone for ArgumentDecorator` — - command_group helper function
-  `clone` function L400-408 — `(&self) -> Self` — - command_group helper function
-  `ArgumentDecorator` type L412-440 — `= ArgumentDecorator` — - command_group helper function
-  `__call__` function L414-439 — `(&self, func: Py<PyAny>) -> PyResult<Py<PyAny>>` — - command_group helper function

#### crates/angreal/src/python_bindings/mod.rs

- pub `decorators` module L11 — `-` — projects to initialize angreal's Python bindings.
- pub `integrations` module L12 — `-` — projects to initialize angreal's Python bindings.
- pub `venv` module L13 — `-` — projects to initialize angreal's Python bindings.
- pub `initialize` function L31-43 — `() -> PyResult<()>` — Initialize angreal's Python bindings
-  `create_angreal_module` function L49-68 — `(py: Python<'_>) -> PyResult<Bound<'_, PyModule>>` — Create the main angreal Python module

#### crates/angreal/src/python_bindings/venv.rs

- pub `VirtualEnv` struct L13-27 — `{ path: PathBuf, name: String, python_executable: PathBuf, python_version: Optio...` — Virtual environment manager
- pub `venv_required` function L476-484 — `( path: &str, requirements: Option<Py<PyAny>>, ) -> PyResult<VenvRequiredDecorat...` — Decorator that wraps a function to run in a virtual environment
- pub `VenvRequiredDecorator` struct L488-491 — `{ path: String, requirements: Option<Py<PyAny>> }` — A Python callable that wraps the venv_required decorator logic
- pub `register_venv` function L634-640 — `(_py: Python, m: &Bound<'_, PyModule>) -> PyResult<()>` — Register the venv module
-  `VirtualEnv` type L30-469 — `= VirtualEnv` — using UV for fast venv creation and package installation.
-  `__new__` function L33-120 — `( path: Option<Py<PyAny>>, python: Option<&str>, requirements: Option<Py<PyAny>>...` — using UV for fast venv creation and package installation.
-  `create` function L122-136 — `(&self) -> PyResult<()>` — using UV for fast venv creation and package installation.
-  `activate` function L138-242 — `(&mut self) -> PyResult<()>` — using UV for fast venv creation and package installation.
-  `remove` function L244-254 — `(&self) -> PyResult<()>` — using UV for fast venv creation and package installation.
-  `__enter__` function L256-261 — `(mut slf: PyRefMut<Self>) -> PyResult<PyRefMut<Self>>` — using UV for fast venv creation and package installation.
-  `__exit__` function L263-271 — `( &mut self, _exc_type: &Bound<'_, PyAny>, _exc_val: &Bound<'_, PyAny>, _exc_tb:...` — using UV for fast venv creation and package installation.
-  `exists` function L275-277 — `(&self, _py: Python) -> PyResult<bool>` — using UV for fast venv creation and package installation.
-  `path` function L281-286 — `(&self, py: Python) -> PyResult<Py<PyAny>>` — using UV for fast venv creation and package installation.
-  `python_executable` function L290-295 — `(&self, py: Python) -> PyResult<Py<PyAny>>` — using UV for fast venv creation and package installation.
-  `deactivate` function L298-345 — `(&mut self) -> PyResult<()>` — using UV for fast venv creation and package installation.
-  `install_requirements` function L348-368 — `(&self) -> PyResult<()>` — using UV for fast venv creation and package installation.
-  `install` function L371-425 — `(&self, packages: Py<PyAny>) -> PyResult<()>` — using UV for fast venv creation and package installation.
-  `discover_available_pythons` function L429-444 — `(_cls: &Bound<'_, PyType>) -> PyResult<Vec<(String, String)>>` — using UV for fast venv creation and package installation.
-  `ensure_python` function L447-457 — `(_cls: &Bound<'_, PyType>, version: &str) -> PyResult<String>` — using UV for fast venv creation and package installation.
-  `version` function L460-468 — `(_cls: &Bound<'_, PyType>) -> PyResult<String>` — using UV for fast venv creation and package installation.
-  `VenvRequiredDecorator` type L494-508 — `= VenvRequiredDecorator` — using UV for fast venv creation and package installation.
-  `__call__` function L495-507 — `(&self, py: Python, func: Py<PyAny>) -> PyResult<Py<PyAny>>` — using UV for fast venv creation and package installation.
-  `VenvRequiredWrapper` struct L511-519 — `{ original_func: Py<PyAny>, path: String, requirements: Option<Py<PyAny>>, comma...` — using UV for fast venv creation and package installation.
-  `VenvRequiredWrapper` type L521-531 — `impl Clone for VenvRequiredWrapper` — using UV for fast venv creation and package installation.
-  `clone` function L522-530 — `(&self) -> Self` — using UV for fast venv creation and package installation.
-  `VenvRequiredWrapper` type L534-631 — `= VenvRequiredWrapper` — using UV for fast venv creation and package installation.
-  `__call__` function L536-570 — `( &self, args: &Bound<'_, pyo3::types::PyTuple>, kwargs: Option<&Bound<'_, pyo3:...` — using UV for fast venv creation and package installation.
-  `get_command` function L574-583 — `(&self) -> PyResult<Py<PyAny>>` — using UV for fast venv creation and package installation.
-  `set_command` function L586-588 — `(&mut self, value: Py<PyAny>)` — using UV for fast venv creation and package installation.
-  `get_arguments` function L592-602 — `(&self) -> PyResult<Py<PyAny>>` — using UV for fast venv creation and package installation.
-  `set_arguments` function L605-607 — `(&mut self, value: Py<PyAny>)` — using UV for fast venv creation and package installation.
-  `__name__` function L610-616 — `(&self) -> PyResult<Py<PyAny>>` — using UV for fast venv creation and package installation.
-  `__doc__` function L619-625 — `(&self) -> PyResult<Py<PyAny>>` — using UV for fast venv creation and package installation.
-  `__getattr__` function L628-630 — `(&self, name: &str) -> PyResult<Py<PyAny>>` — using UV for fast venv creation and package installation.

### crates/angreal/src/python_bindings/integrations

> *Semantic summary to be generated by AI agent.*

#### crates/angreal/src/python_bindings/integrations/compose.rs

- pub `PyDockerCompose` struct L13-15 — `{ inner: RustDockerCompose }` — Python wrapper for Docker Compose operations
- pub `PyComposeOutput` struct L20-29 — `{ success: bool, exit_code: i32, stdout: String, stderr: String }` — Python wrapper for compose command output
- pub `compose` function L333-335 — `(file: &str, project_name: Option<&str>) -> PyResult<PyDockerCompose>` — Convenience function to create a DockerCompose instance
- pub `compose_integration` function L339-344 — `(_py: Python, m: &Bound<'_, PyModule>) -> PyResult<()>` — Compose integration module
-  `PyComposeOutput` type L31-40 — `= PyComposeOutput` — PyO3 bindings for Docker Compose integration
-  `from` function L32-39 — `(output: ComposeOutput) -> Self` — PyO3 bindings for Docker Compose integration
-  `PyDockerCompose` type L43-328 — `= PyDockerCompose` — PyO3 bindings for Docker Compose integration
-  `new` function L47-57 — `(file: &str, project_name: Option<&str>) -> PyResult<Self>` — Create a new DockerCompose instance
-  `is_available` function L61-63 — `() -> bool` — Check if Docker Compose is available
-  `compose_file` function L67-69 — `(&self) -> String` — Get the compose file path
-  `working_dir` function L73-75 — `(&self) -> String` — Get the working directory
-  `project_name` function L79-81 — `(&self) -> Option<String>` — Get the project name
-  `up` function L85-109 — `( &self, detach: bool, build: bool, remove_orphans: bool, force_recreate: bool, ...` — Start services (docker-compose up)
-  `down` function L113-131 — `( &self, volumes: bool, remove_orphans: bool, timeout: Option<String>, ) -> PyRe...` — Stop and remove services (docker-compose down)
-  `restart` function L135-150 — `( &self, services: Option<Vec<String>>, timeout: Option<String>, ) -> PyResult<P...` — Restart services (docker-compose restart)
-  `logs` function L154-176 — `( &self, services: Option<Vec<String>>, follow: bool, timestamps: bool, tail: Op...` — View service logs (docker-compose logs)
-  `ps` function L180-200 — `( &self, all: bool, quiet: bool, services: bool, filter_services: Option<Vec<Str...` — List running services (docker-compose ps)
-  `build` function L204-224 — `( &self, services: Option<Vec<String>>, no_cache: bool, pull: bool, parallel: bo...` — Build services (docker-compose build)
-  `start` function L228-236 — `(&self, services: Option<Vec<String>>) -> PyResult<PyComposeOutput>` — Start services (docker-compose start)
-  `stop` function L240-256 — `( &self, services: Option<Vec<String>>, timeout: Option<String>, ) -> PyResult<P...` — Stop services (docker-compose stop)
-  `exec` function L261-289 — `( &self, service: &str, command: Vec<String>, detach: bool, tty: bool, user: Opt...` — Execute a command in a service container (docker-compose exec)
-  `pull` function L293-301 — `(&self, services: Option<Vec<String>>) -> PyResult<PyComposeOutput>` — Pull service images (docker-compose pull)
-  `config` function L305-318 — `(&self, quiet: bool, services: bool, volumes: bool) -> PyResult<PyComposeOutput>` — Validate and view the compose configuration (docker-compose config)
-  `__repr__` function L321-327 — `(&self) -> String` — String representation

#### crates/angreal/src/python_bindings/integrations/docker.rs

- pub `docker_integration` function L11-24 — `(_py: Python, m: &Bound<'_, PyModule>) -> PyResult<()>` — Docker integration module

#### crates/angreal/src/python_bindings/integrations/flox.rs

- pub `Flox` struct L15-26 — `{ path: PathBuf, _is_activated: bool, _original_env: Option<HashMap<String, Stri...` — Flox environment manager
- pub `ServiceInfo` struct L307-314 — `{ name: String, status: String, pid: Option<u32> }` — Information about a single service
- pub `FloxServices` struct L347-350 — `{ path: PathBuf }` — Flox services manager
- pub `FloxServiceHandle` struct L558-565 — `{ flox_env_path: PathBuf, services: Vec<ServiceInfo>, started_at: String }` — Handle to started services for persistence and cleanup
- pub `flox_required` function L780-790 — `( path: Option<Py<PyAny>>, services: Option<Vec<String>>, ) -> PyResult<FloxRequ...` — Decorator that wraps a function to run in a Flox environment
- pub `FloxRequiredDecorator` struct L794-797 — `{ path: Option<Py<PyAny>>, services: Option<Vec<String>> }` — A Python callable that wraps the flox_required decorator logic
- pub `flox` function L928-937 — `(_py: Python, m: &Bound<'_, PyModule>) -> PyResult<()>` — Create the flox submodule
- pub `register_flox` function L940-949 — `(_py: Python, m: &Bound<'_, PyModule>) -> PyResult<()>` — Register the flox module (for use in parent module)
-  `Flox` type L29-302 — `= Flox` — This module provides the flox submodule for angreal.integrations.flox
-  `__new__` function L32-92 — `(path: Option<Py<PyAny>>, services: Option<Vec<String>>) -> PyResult<Self>` — This module provides the flox submodule for angreal.integrations.flox
-  `exists` function L96-99 — `(&self) -> bool` — Check if the Flox environment exists (.flox/ directory)
-  `has_manifest` function L103-106 — `(&self) -> bool` — Check if the manifest.toml exists
-  `path` function L110-115 — `(&self, py: Python) -> PyResult<Py<PyAny>>` — Get the path as a Python Path object
-  `activate` function L121-175 — `(&mut self) -> PyResult<()>` — Activate the Flox environment
-  `deactivate` function L180-210 — `(&mut self) -> PyResult<()>` — Deactivate the Flox environment
-  `__enter__` function L213-231 — `(mut slf: PyRefMut<Self>) -> PyResult<PyRefMut<Self>>` — Context manager entry - activates environment and starts services
-  `__exit__` function L234-251 — `( &mut self, _exc_type: &Bound<'_, PyAny>, _exc_val: &Bound<'_, PyAny>, _exc_tb:...` — Context manager exit - stops services and deactivates environment
-  `is_available` function L255-257 — `(_cls: &Bound<'_, PyType>) -> bool` — Check if the Flox CLI is available
-  `version` function L261-268 — `(_cls: &Bound<'_, PyType>) -> PyResult<String>` — Get the Flox version string
-  `run` function L274-293 — `(&self, command: &str, args: Option<Vec<String>>) -> PyResult<(i32, String, Stri...` — Run a command within the Flox environment
-  `services` function L297-301 — `(&self) -> FloxServices` — Get a FloxServices manager for this environment
-  `ServiceInfo` type L317-341 — `= ServiceInfo` — This module provides the flox submodule for angreal.integrations.flox
-  `__new__` function L320-322 — `(name: String, status: String, pid: Option<u32>) -> Self` — This module provides the flox submodule for angreal.integrations.flox
-  `__repr__` function L324-335 — `(&self) -> String` — This module provides the flox submodule for angreal.integrations.flox
-  `as_tuple` function L338-340 — `(&self) -> (String, String, Option<u32>)` — Convert to a tuple (name, status, pid)
-  `FloxServices` type L353-551 — `= FloxServices` — This module provides the flox submodule for angreal.integrations.flox
-  `__new__` function L355-405 — `(path: Py<PyAny>) -> PyResult<Self>` — This module provides the flox submodule for angreal.integrations.flox
-  `path` function L409-414 — `(&self, py: Python) -> PyResult<Py<PyAny>>` — Get the path as a Python Path object
-  `start` function L421-466 — `(&self, services: &Bound<'_, PyAny>) -> PyResult<FloxServiceHandle>` — Start services
-  `stop` function L472-489 — `(&self, services: &Bound<'_, PyAny>) -> PyResult<()>` — Stop services
-  `status` function L494-511 — `(&self) -> PyResult<Vec<ServiceInfo>>` — Get status of all services
-  `logs` function L515-529 — `(&self, service: &str, follow: bool, tail: Option<u32>) -> PyResult<String>` — Get logs for a specific service
-  `restart` function L533-550 — `(&self, services: &Bound<'_, PyAny>) -> PyResult<()>` — Restart services
-  `chrono_now` function L568-625 — `() -> String` — Get current timestamp as ISO 8601 string
-  `is_leap_year` function L627-629 — `(y: i64) -> bool` — This module provides the flox submodule for angreal.integrations.flox
-  `FloxServiceHandle` type L632-773 — `= FloxServiceHandle` — This module provides the flox submodule for angreal.integrations.flox
-  `flox_env_path` function L635-640 — `(&self, py: Python) -> PyResult<Py<PyAny>>` — Get the Flox environment path
-  `services` function L644-646 — `(&self) -> Vec<ServiceInfo>` — Get list of services
-  `started_at` function L650-652 — `(&self) -> String` — Get the started_at timestamp
-  `stop` function L655-664 — `(&self) -> PyResult<()>` — Stop only the services tracked by this handle
-  `save` function L668-698 — `(&self, path: Option<&str>) -> PyResult<()>` — Save handle to a JSON file
-  `load` function L703-764 — `(_cls: &Bound<'_, PyType>, path: Option<&str>) -> PyResult<FloxServiceHandle>` — Load handle from a JSON file
-  `__repr__` function L766-772 — `(&self) -> String` — This module provides the flox submodule for angreal.integrations.flox
-  `FloxRequiredDecorator` type L800-812 — `= FloxRequiredDecorator` — This module provides the flox submodule for angreal.integrations.flox
-  `__call__` function L801-811 — `(&self, py: Python, func: Py<PyAny>) -> PyResult<Py<PyAny>>` — This module provides the flox submodule for angreal.integrations.flox
-  `FloxRequiredWrapper` struct L815-819 — `{ original_func: Py<PyAny>, path: Option<Py<PyAny>>, services: Option<Vec<String...` — This module provides the flox submodule for angreal.integrations.flox
-  `FloxRequiredWrapper` type L821-829 — `impl Clone for FloxRequiredWrapper` — This module provides the flox submodule for angreal.integrations.flox
-  `clone` function L822-828 — `(&self) -> Self` — This module provides the flox submodule for angreal.integrations.flox
-  `FloxRequiredWrapper` type L832-924 — `= FloxRequiredWrapper` — This module provides the flox submodule for angreal.integrations.flox
-  `__call__` function L834-889 — `( &self, args: &Bound<'_, pyo3::types::PyTuple>, kwargs: Option<&Bound<'_, pyo3:...` — This module provides the flox submodule for angreal.integrations.flox
-  `get_arguments` function L894-900 — `(&self) -> PyResult<Py<PyAny>>` — This module provides the flox submodule for angreal.integrations.flox
-  `__name__` function L903-909 — `(&self) -> PyResult<Py<PyAny>>` — This module provides the flox submodule for angreal.integrations.flox
-  `__doc__` function L912-918 — `(&self) -> PyResult<Py<PyAny>>` — This module provides the flox submodule for angreal.integrations.flox
-  `__getattr__` function L921-923 — `(&self, name: &str) -> PyResult<Py<PyAny>>` — This module provides the flox submodule for angreal.integrations.flox

#### crates/angreal/src/python_bindings/integrations/git.rs

- pub `GitException` struct L14-16 — `{ message: String }` — Git integration bindings
- pub `PyGit` struct L31-33 — `{ inner: Git }` — Git integration bindings
- pub `git_clone` function L335-339 — `(remote: &str, destination: Option<&str>) -> PyResult<String>` — Git integration bindings
- pub `git_integration` function L345-352 — `(_py: Python, m: &Bound<'_, PyModule>) -> PyResult<()>` — Git integration module
-  `GitException` type L19-28 — `= GitException` — Git integration bindings
-  `new` function L21-23 — `(message: String) -> Self` — Git integration bindings
-  `__str__` function L25-27 — `(&self) -> String` — Git integration bindings
-  `PyGit` type L36-331 — `= PyGit` — Git integration bindings
-  `new` function L39-43 — `(working_dir: Option<&str>) -> PyResult<Self>` — Git integration bindings
-  `execute` function L45-62 — `( &self, subcommand: &str, args: Vec<String>, ) -> PyResult<(i32, Py<PyAny>, Py<...` — Git integration bindings
-  `init` function L65-79 — `(&self, bare: Option<bool>) -> PyResult<(i32, Py<PyAny>, Py<PyAny>)>` — Git integration bindings
-  `add` function L82-102 — `( &self, paths: &Bound<'_, pyo3::types::PyTuple>, ) -> PyResult<(i32, Py<PyAny>,...` — Git integration bindings
-  `commit` function L105-122 — `(&self, message: &str, all: Option<bool>) -> PyResult<(i32, Py<PyAny>, Py<PyAny>...` — Git integration bindings
-  `push` function L125-148 — `( &self, remote: Option<&str>, branch: Option<&str>, ) -> PyResult<(i32, Py<PyAn...` — Git integration bindings
-  `pull` function L151-174 — `( &self, remote: Option<&str>, branch: Option<&str>, ) -> PyResult<(i32, Py<PyAn...` — Git integration bindings
-  `status` function L177-194 — `(&self, short: Option<bool>) -> PyResult<(i32, Py<PyAny>, Py<PyAny>)>` — Git integration bindings
-  `branch` function L197-220 — `( &self, name: Option<&str>, delete: Option<bool>, ) -> PyResult<(i32, Py<PyAny>...` — Git integration bindings
-  `checkout` function L223-244 — `( &self, branch: &str, create: Option<bool>, ) -> PyResult<(i32, Py<PyAny>, Py<P...` — Git integration bindings
-  `tag` function L247-264 — `(&self, name: &str, message: Option<&str>) -> PyResult<(i32, Py<PyAny>, Py<PyAny...` — Git integration bindings
-  `__call__` function L267-317 — `( &self, command: &str, args: &Bound<'_, pyo3::types::PyTuple>, kwargs: Option<&...` — Git integration bindings
-  `working_dir` function L320-322 — `(&self) -> String` — Git integration bindings
-  `__getattr__` function L324-330 — `(&self, _py: Python, name: &str) -> PyResult<Py<PyAny>>` — Git integration bindings

#### crates/angreal/src/python_bindings/integrations/mod.rs

- pub `compose` module L6 — `-` — Integration modules for external tools and services
- pub `docker` module L7 — `-` — Integration modules for external tools and services
- pub `flox` module L8 — `-` — Integration modules for external tools and services
- pub `git` module L9 — `-` — Integration modules for external tools and services
- pub `venv` module L10 — `-` — Integration modules for external tools and services
- pub `integrations` function L16-39 — `(py: Python, m: &Bound<'_, PyModule>) -> PyResult<()>` — Create the integrations submodule

#### crates/angreal/src/python_bindings/integrations/venv.rs

- pub `venv` function L9-13 — `(_py: Python, m: &Bound<'_, PyModule>) -> PyResult<()>` — Create the venv submodule

### crates/angreal/src/validation

> *Semantic summary to be generated by AI agent.*

#### crates/angreal/src/validation/allowed_values.rs

- pub `allowed_values` function L13-49 — `(input: &str, allowed_values: &Value) -> Result<(), String>` — Validate that input is one of the allowed values
-  `tests` module L52-109 — `-` — Validation for allowed values
-  `test_allowed_values_valid` function L57-69 — `()` — Validation for allowed values
-  `test_allowed_values_invalid` function L72-88 — `()` — Validation for allowed values
-  `test_allowed_values_numbers` function L91-108 — `()` — Validation for allowed values

#### crates/angreal/src/validation/length.rs

- pub `length_min` function L13-26 — `(input: &str, min_length: &Value) -> Result<(), String>` — Validate that input string has at least the minimum length
- pub `length_max` function L37-47 — `(input: &str, max_length: &Value) -> Result<(), String>` — Validate that input string does not exceed maximum length
-  `tests` module L50-117 — `-` — Validation for string length
-  `test_length_min_valid` function L55-67 — `()` — Validation for string length
-  `test_length_min_invalid_type` function L70-77 — `()` — Validation for string length
-  `test_length_max_valid` function L80-93 — `()` — Validation for string length
-  `test_length_max_invalid_type` function L96-103 — `()` — Validation for string length
-  `test_length_unicode` function L106-116 — `()` — Validation for string length

#### crates/angreal/src/validation/max.rs

- pub `max` function L13-41 — `(input: &str, max_value: &Value) -> Result<(), String>` — Validate that input is less than or equal to the maximum value
-  `tests` module L44-81 — `-` — Validation for maximum value
-  `test_max_integers` function L49-63 — `()` — Validation for maximum value
-  `test_max_floats` function L66-80 — `()` — Validation for maximum value

#### crates/angreal/src/validation/min.rs

- pub `min` function L13-47 — `(input: &str, min_value: &Value) -> Result<(), String>` — Validate that input is greater than or equal to the minimum value
-  `tests` module L50-87 — `-` — Validation for minimum value
-  `test_min_integers` function L55-69 — `()` — Validation for minimum value
-  `test_min_floats` function L72-86 — `()` — Validation for minimum value

#### crates/angreal/src/validation/mod.rs

- pub `validate_input` function L32-116 — `(input: &str, key: &str, validation_table: &Table) -> Result<(), String>` — Validate input against validation rules defined in TOML
-  `allowed_values` module L6 — `-` — Validation utilities for template variables
-  `length` module L7 — `-` — Validation utilities for template variables
-  `max` module L8 — `-` — Validation utilities for template variables
-  `min` module L9 — `-` — Validation utilities for template variables
-  `not_empty` module L10 — `-` — Validation utilities for template variables
-  `regex_match` module L11 — `-` — Validation utilities for template variables
-  `type_check` module L12 — `-` — Validation utilities for template variables
-  `tests` module L119-306 — `-` — Validation utilities for template variables
-  `test_validate_input_allowed_values` function L124-147 — `()` — Validation utilities for template variables
-  `test_validate_input_min_max` function L150-170 — `()` — Validation utilities for template variables
-  `test_validate_input_regex` function L173-189 — `()` — Validation utilities for template variables
-  `test_validate_input_multiple_rules` function L192-220 — `()` — Validation utilities for template variables
-  `test_validate_not_empty` function L223-237 — `()` — Validation utilities for template variables
-  `test_validate_type` function L240-266 — `()` — Validation utilities for template variables
-  `test_validate_length` function L269-305 — `()` — Validation utilities for template variables

#### crates/angreal/src/validation/not_empty.rs

- pub `not_empty` function L13-27 — `(input: &str, value: &Value) -> Result<(), String>` — Validate that input is not empty
-  `tests` module L30-54 — `-` — Validation for non-empty values
-  `test_not_empty_valid` function L35-41 — `()` — Validation for non-empty values
-  `test_not_empty_invalid` function L44-53 — `()` — Validation for non-empty values

#### crates/angreal/src/validation/regex_match.rs

- pub `regex_match` function L14-29 — `(input: &str, pattern: &Value) -> Result<(), String>` — Validate that input matches the regex pattern
-  `tests` module L32-74 — `-` — Validation for regex pattern matching
-  `test_regex_match_email` function L37-50 — `()` — Validation for regex pattern matching
-  `test_regex_match_numeric` function L53-63 — `()` — Validation for regex pattern matching
-  `test_invalid_regex` function L66-73 — `()` — Validation for regex pattern matching

#### crates/angreal/src/validation/type_check.rs

- pub `type_check` function L13-47 — `(input: &str, type_value: &Value) -> Result<(), String>` — Validate that input matches the specified type
-  `tests` module L50-121 — `-` — Validation for type checking
-  `test_type_integer` function L55-69 — `()` — Validation for type checking
-  `test_type_float` function L72-84 — `()` — Validation for type checking
-  `test_type_boolean` function L87-100 — `()` — Validation for type checking
-  `test_type_string` function L103-110 — `()` — Validation for type checking
-  `test_type_unknown` function L113-120 — `()` — Validation for type checking

### crates/angreal/tests/common

> *Semantic summary to be generated by AI agent.*

#### crates/angreal/tests/common/mod.rs

- pub `generate_random_string` function L6-18 — `() -> String`
- pub `make_tmp_dir` function L20-28 — `() -> PathBuf`
-  `CHARSET` variable L7 — `: &[u8]`
-  `RSTRING_LEN` variable L8 — `: usize`

### crates/angreal/tests/common/test_assets

> *Semantic summary to be generated by AI agent.*

#### crates/angreal/tests/common/test_assets/exception_init.py

- pub `task` function L1-2 — `def task()`

#### crates/angreal/tests/common/test_assets/exception_task.py

- pub `task` function L1-2 — `def task()`

#### crates/angreal/tests/common/test_assets/good_task.py

- pub `task` function L4-8 — `def task()`

#### crates/angreal/tests/common/test_assets/no_func_init.py

- pub `not_valid_sig` function L1-2 — `def not_valid_sig()`

#### crates/angreal/tests/common/test_assets/no_func_task.py

- pub `not_valid_sig` function L1-2 — `def not_valid_sig()`

### crates/angreal/tests/integration/git

> *Semantic summary to be generated by AI agent.*

#### crates/angreal/tests/integration/git/mod.rs

-  `utils` module L5 — `-`
-  `remote_tests` module L9-26 — `-` — Test suite for remote repository existence checks
-  `test_repo_exists` function L13-16 — `()`
-  `test_repo_no_exists` function L19-25 — `()`
-  `clone_tests` module L30-92 — `-` — Test suite for repository cloning operations
-  `test_clone_public` function L34-45 — `()`
-  `test_clone_private` function L51-57 — `()` — Test cloning a private repository (requires authentication)
-  `test_clone_here` function L60-91 — `()`
-  `pull_tests` module L96-126 — `-` — Test suite for pull operations
-  `test_git_pull_ff` function L100-112 — `()`
-  `test_git_pull_ff_private` function L118-125 — `()` — Test pulling from a private repository (requires authentication)

#### crates/angreal/tests/integration/git/utils.rs

- pub `GitTestFixture` struct L6-9 — `{ temp_dir: TempDir, original_dir: PathBuf }` — A test fixture that manages temporary directories and working directory state
- pub `new` function L13-21 — `() -> Self` — Creates a new test fixture with a unique temporary directory
- pub `temp_path` function L24-26 — `(&self) -> &Path` — Returns the path to the temporary directory
- pub `change_to_temp_dir` function L29-32 — `(&self)` — Changes the working directory to the temporary directory
- pub `create_subdir` function L35-39 — `(&self, name: &str) -> PathBuf` — Creates a subdirectory in the temporary directory
- pub `cleanup` function L42-46 — `(&self)` — Cleans up the temporary directory
-  `GitTestFixture` type L11-47 — `= GitTestFixture`
-  `GitTestFixture` type L49-57 — `impl Drop for GitTestFixture`
-  `drop` function L50-56 — `(&mut self)`

### crates/angreal/tests/integration

> *Semantic summary to be generated by AI agent.*

#### crates/angreal/tests/integration/init.rs

-  `test_init_from_git` function L8-20 — `()`
-  `test_init_long` function L23-42 — `()`
-  `test_init_values` function L45-67 — `()`
-  `test_init_short` function L70-78 — `()`
-  `test_render_template` function L81-112 — `()`
-  `test_render_template_values` function L115-149 — `()`

#### crates/angreal/tests/integration/lib.rs

- pub `git` module L1 — `-`
- pub `init` module L2 — `-`

#### crates/angreal/tests/integration/utils.rs

-  `test_get_context` function L14-59 — `()`
-  `test_get_context_no_angreal_dir` function L62-77 — `()`
-  `test_get_context_no_angreal_toml` function L80-99 — `()`

### py_tests

> *Semantic summary to be generated by AI agent.*

#### py_tests/test_angreal.py

- pub `func` function L8-9 — `def func()`
- pub `test_generate_context` function L11-18 — `def test_generate_context()`
- pub `test_render_directory` function L20-32 — `def test_render_directory()`
- pub `test_render_template` function L34-37 — `def test_render_template()`
- pub `test_angreal_get_root` function L39-41 — `def test_angreal_get_root()` — test that get root returns something useful
- pub `test_angreal_required_good` function L43-45 — `def test_angreal_required_good()` — test required version equal to current version
- pub `test_angreal_required_bad` function L47-50 — `def test_angreal_required_bad()` — test required version can't be greater than current version
- pub `test_group_init` function L52-57 — `def test_group_init()` — Test group initialization
- pub `test_cmd_init` function L60-86 — `def test_cmd_init()` — Test command initialization
- pub `test_arg_init` function L89-147 — `def test_arg_init()` — Test arg initialization
- pub `test_group` function L150-164 — `def test_group()`

#### py_tests/test_entrypoint_registration.py

- pub `TestEntrypointRegistration` class L10-226 — `{ setup_method, teardown_method, test_register_entrypoint, test_list_entrypoints...` — Test the entrypoint registration functions.
- pub `setup_method` method L13-28 — `def setup_method(self)` — Set up test environment with temporary home directory.
- pub `teardown_method` method L30-49 — `def teardown_method(self)` — Clean up test environment.
- pub `test_register_entrypoint` method L51-79 — `def test_register_entrypoint(self)` — Test basic entrypoint registration.
- pub `test_list_entrypoints` method L81-94 — `def test_list_entrypoints(self)` — Test listing registered entrypoints.
- pub `test_unregister_entrypoint` method L96-115 — `def test_unregister_entrypoint(self)` — Test unregistering an entrypoint.
- pub `test_cleanup_entrypoints` method L117-139 — `def test_cleanup_entrypoints(self)` — Test cleaning up all entrypoints.
- pub `test_register_duplicate_fails` method L141-156 — `def test_register_duplicate_fails(self)` — Test that registering a duplicate entrypoint fails.
- pub `test_script_permissions_unix` method L158-173 — `def test_script_permissions_unix(self)` — Test that created scripts have executable permissions on Unix.
- pub `test_script_functionality` method L175-200 — `def test_script_functionality(self)` — Test that the generated script would work correctly.
- pub `test_registry_persistence` method L202-217 — `def test_registry_persistence(self)` — Test that the registry persists between function calls.
- pub `test_unregister_nonexistent` method L219-226 — `def test_unregister_nonexistent(self)` — Test unregistering a non-existent entrypoint doesn't fail.

#### py_tests/test_exit_codes.py

- pub `test_return_integer_zero` function L17-20 — `def test_return_integer_zero()` — Integer return of 0 should exit 0.
- pub `test_return_integer_nonzero` function L23-26 — `def test_return_integer_nonzero()` — Integer return of 42 should exit 42.
- pub `test_return_true` function L29-32 — `def test_return_true()` — Boolean True should exit 0.
- pub `test_return_false` function L35-38 — `def test_return_false()` — Boolean False should exit 1.
- pub `test_return_none` function L41-44 — `def test_return_none()` — None return should exit 0.
- pub `test_sys_exit_zero` function L47-50 — `def test_sys_exit_zero()` — sys.exit(0) should exit 0.
- pub `test_sys_exit_nonzero` function L53-56 — `def test_sys_exit_nonzero()` — sys.exit(3) should exit 3.
- pub `test_exception_exits_56` function L59-62 — `def test_exception_exits_56()` — Unhandled exception should exit 56.
-  `_run` function L9-14 — `def _run(*args)`

#### py_tests/test_functional.py

- pub `test_group_1` function L8-26 — `def test_group_1()` — test a basic nested command with flag
- pub `test_group_2` function L29-48 — `def test_group_2()` — test a multi nested command with flag
- pub `test_task_verbose_flag` function L51-81 — `def test_task_verbose_flag()` — test that a task can define --verbose / -v flags

#### py_tests/test_get_context.py

- pub `test_get_context` function L6-13 — `def test_get_context()` — Test retrieving context from .angreal/angreal.toml
- pub `test_get_context_no_angreal_dir` function L16-32 — `def test_get_context_no_angreal_dir()` — Test get_context when no .angreal directory exists

#### py_tests/test_git_rust_integration.py

- pub `TestGitRustIntegration` class L24-144 — `{ test_git_init, test_git_workflow, test_git_call_syntax, test_git_method_syntax...` — Test the Rust-based git integration.
- pub `test_git_init` method L27-34 — `def test_git_init(self)` — Test git repository initialization.
- pub `test_git_workflow` method L36-61 — `def test_git_workflow(self)` — Test a complete git workflow.
- pub `test_git_call_syntax` method L63-71 — `def test_git_call_syntax(self)` — Test the __call__ syntax for backwards compatibility.
- pub `test_git_method_syntax` method L73-85 — `def test_git_method_syntax(self)` — Test the method syntax like git.add().
- pub `test_git_convenience_methods` method L87-109 — `def test_git_convenience_methods(self)` — Test the high-level convenience methods.
- pub `test_git_error_handling` method L111-118 — `def test_git_error_handling(self)` — Test that git errors are properly handled.
- pub `test_git_with_options` method L120-131 — `def test_git_with_options(self)` — Test git commands with options (kwargs).
- pub `test_original_api_compatibility` method L133-144 — `def test_original_api_compatibility(self)` — Test that the API matches the updated implementation.

### py_tests/integrations

> *Semantic summary to be generated by AI agent.*

#### py_tests/integrations/test_docker_compose.py

- pub `TestDockerCompose` class L40-212 — `{ test_is_available, test_compose_function, test_compose_function_with_project_n...` — Test Docker Compose integration
- pub `test_is_available` method L43-47 — `def test_is_available(self)` — Test if Docker Compose availability check works
- pub `test_compose_function` method L49-57 — `def test_compose_function(self, sample_compose_file)` — Test the compose convenience function
- pub `test_compose_function_with_project_name` method L59-66 — `def test_compose_function_with_project_name(self, sample_compose_file)` — Test the compose function with a project name
- pub `test_docker_compose_init` method L68-77 — `def test_docker_compose_init(self, sample_compose_file)` — Test DockerCompose initialization
- pub `test_docker_compose_init_with_project_name` method L79-85 — `def test_docker_compose_init_with_project_name(self, sample_compose_file)` — Test DockerCompose initialization with project name
- pub `test_invalid_compose_file` method L87-90 — `def test_invalid_compose_file(self, invalid_compose_file)` — Test error handling for invalid compose file
- pub `test_config_command` method L92-107 — `def test_config_command(self, sample_compose_file)` — Test docker-compose config command
- pub `test_config_command_with_options` method L109-120 — `def test_config_command_with_options(self, sample_compose_file)` — Test docker-compose config command with options
- pub `test_ps_command` method L122-132 — `def test_ps_command(self, sample_compose_file)` — Test docker-compose ps command
- pub `test_ps_command_with_options` method L134-142 — `def test_ps_command_with_options(self, sample_compose_file)` — Test docker-compose ps command with options
- pub `test_build_command` method L144-154 — `def test_build_command(self, sample_compose_file)` — Test docker-compose build command
- pub `test_pull_command` method L156-166 — `def test_pull_command(self, sample_compose_file)` — Test docker-compose pull command
- pub `test_logs_command` method L168-177 — `def test_logs_command(self, sample_compose_file)` — Test docker-compose logs command
- pub `test_exec_command_validation` method L179-188 — `def test_exec_command_validation(self, sample_compose_file)` — Test docker-compose exec command validation
- pub `test_exec_command` method L190-200 — `def test_exec_command(self, sample_compose_file)` — Test docker-compose exec command
- pub `test_repr` method L202-212 — `def test_repr(self, sample_compose_file)` — Test string representation
- pub `TestComposeResult` class L215-236 — `{ test_compose_result_attributes }` — Test ComposeResult class functionality
- pub `test_compose_result_attributes` method L218-236 — `def test_compose_result_attributes(self, sample_compose_file)` — Test that ComposeResult has expected attributes
- pub `TestIntegrationAPI` class L239-307 — `{ test_api_usage_pattern, test_service_specific_operations, test_build_options, ...` — Test the high-level API as described in requirements
- pub `test_api_usage_pattern` method L242-262 — `def test_api_usage_pattern(self, sample_compose_file)` — Test the API usage pattern from requirements
- pub `test_service_specific_operations` method L264-281 — `def test_service_specific_operations(self, sample_compose_file)` — Test operations on specific services
- pub `test_build_options` method L283-296 — `def test_build_options(self, sample_compose_file)` — Test build-related options
- pub `test_volume_management` method L298-307 — `def test_volume_management(self, sample_compose_file)` — Test volume-related operations
- pub `TestDockerComposeIntegration` class L317-352 — `{ test_full_lifecycle }` — Integration tests that require Docker Compose to be available
- pub `test_full_lifecycle` method L320-352 — `def test_full_lifecycle(self, sample_compose_file)` — Test a full container lifecycle (if Docker is available)

#### py_tests/integrations/test_flox.py

- pub `TestFloxClass` class L17-100 — `{ test_flox_init_default_path, test_flox_init_with_string_path, test_flox_init_w...` — Unit tests for the Flox class.
- pub `test_flox_init_default_path` method L20-24 — `def test_flox_init_default_path(self)` — Test Flox initialization with default path (current directory).
- pub `test_flox_init_with_string_path` method L26-30 — `def test_flox_init_with_string_path(self)` — Test Flox initialization with a string path.
- pub `test_flox_init_with_path_object` method L32-37 — `def test_flox_init_with_path_object(self)` — Test Flox initialization with a Path object.
- pub `test_flox_init_relative_path` method L39-42 — `def test_flox_init_relative_path(self)` — Test Flox initialization with a relative path.
- pub `test_exists_property_false_when_no_flox_env` method L44-48 — `def test_exists_property_false_when_no_flox_env(self)` — Test that exists returns False when no .flox directory.
- pub `test_exists_property_true_when_flox_env_present` method L50-58 — `def test_exists_property_true_when_flox_env_present(self)` — Test that exists returns True when .flox directory exists.
- pub `test_has_manifest_property_false` method L60-64 — `def test_has_manifest_property_false(self)` — Test has_manifest returns False when manifest.toml doesn't exist.
- pub `test_has_manifest_property_true` method L66-75 — `def test_has_manifest_property_true(self)` — Test has_manifest returns True when manifest.toml exists.
- pub `test_path_property_returns_path_object` method L77-81 — `def test_path_property_returns_path_object(self)` — Test that path property returns a pathlib.Path object.
- pub `test_is_activated_initially_false` method L83-86 — `def test_is_activated_initially_false(self)` — Test that _is_activated is False before activation.
- pub `test_activate_fails_without_flox_env` method L88-93 — `def test_activate_fails_without_flox_env(self)` — Test that activate raises error when no .flox directory.
- pub `test_deactivate_noop_when_not_activated` method L95-100 — `def test_deactivate_noop_when_not_activated(self)` — Test that deactivate is a no-op when not activated.
- pub `TestFloxClassMethods` class L103-128 — `{ test_is_available_returns_bool, test_version_returns_string, test_version_rais...` — Tests for Flox classmethods.
- pub `test_is_available_returns_bool` method L106-109 — `def test_is_available_returns_bool(self)` — Test that is_available returns a boolean.
- pub `TestFloxActivation` class L135-227 — `{ flox_env, test_activate_deactivate_cycle, test_double_activate_is_noop, test_d...` — Integration tests for Flox activation (requires Flox CLI).
- pub `test_activate_deactivate_cycle` method L158-175 — `def test_activate_deactivate_cycle(self, flox_env)` — Test basic activate/deactivate cycle.
- pub `test_double_activate_is_noop` method L177-188 — `def test_double_activate_is_noop(self, flox_env)` — Test that activating twice is a no-op.
- pub `test_double_deactivate_is_noop` method L190-201 — `def test_double_deactivate_is_noop(self, flox_env)` — Test that deactivating twice is a no-op.
- pub `test_context_manager` method L203-213 — `def test_context_manager(self, flox_env)` — Test using Flox as a context manager.
- pub `test_context_manager_exception_handling` method L215-227 — `def test_context_manager_exception_handling(self, flox_env)` — Test that context manager deactivates even on exception.
- pub `TestFloxRun` class L234-278 — `{ flox_env, test_run_simple_command, test_run_command_without_args, test_run_fai...` — Tests for running commands in Flox environment.
- pub `test_run_simple_command` method L254-261 — `def test_run_simple_command(self, flox_env)` — Test running a simple command in Flox environment.
- pub `test_run_command_without_args` method L263-270 — `def test_run_command_without_args(self, flox_env)` — Test running a command without arguments.
- pub `test_run_failing_command` method L272-278 — `def test_run_failing_command(self, flox_env)` — Test running a command that fails.
- pub `TestFloxApiAvailability` class L281-324 — `{ test_flox_has_expected_properties, test_flox_has_expected_methods, test_flox_h...` — Test that all expected API methods exist.
- pub `test_flox_has_expected_properties` method L284-291 — `def test_flox_has_expected_properties(self)` — Test that Flox has all expected properties.
- pub `test_flox_has_expected_methods` method L293-302 — `def test_flox_has_expected_methods(self)` — Test that Flox has all expected methods.
- pub `test_flox_has_expected_classmethods` method L304-309 — `def test_flox_has_expected_classmethods(self)` — Test that Flox has all expected classmethods.
- pub `test_flox_has_context_manager_methods` method L311-316 — `def test_flox_has_context_manager_methods(self)` — Test that Flox has context manager methods.
- pub `test_flox_has_services_property` method L318-324 — `def test_flox_has_services_property(self)` — Test that Flox has services property.
- pub `TestFloxServicesClass` class L327-363 — `{ test_flox_services_init_with_string_path, test_flox_services_init_with_path_ob...` — Unit tests for the FloxServices class.
- pub `test_flox_services_init_with_string_path` method L330-334 — `def test_flox_services_init_with_string_path(self)` — Test FloxServices initialization with string path.
- pub `test_flox_services_init_with_path_object` method L336-341 — `def test_flox_services_init_with_path_object(self)` — Test FloxServices initialization with Path object.
- pub `test_flox_services_path_property` method L343-347 — `def test_flox_services_path_property(self)` — Test that path property returns Path object.
- pub `test_flox_services_has_expected_methods` method L349-363 — `def test_flox_services_has_expected_methods(self)` — Test that FloxServices has all expected methods.
- pub `TestServiceInfoClass` class L366-378 — `{ test_service_info_repr, test_service_info_as_tuple_api }` — Unit tests for the ServiceInfo class.
- pub `test_service_info_repr` method L369-373 — `def test_service_info_repr(self)` — Test ServiceInfo repr method works (via FloxServiceHandle).
- pub `test_service_info_as_tuple_api` method L375-378 — `def test_service_info_as_tuple_api(self)` — Test that ServiceInfo has as_tuple method (via internal API).
- pub `TestFloxServiceHandleClass` class L381-472 — `{ test_service_handle_has_expected_properties, test_service_handle_save_and_load...` — Unit tests for the FloxServiceHandle class.
- pub `test_service_handle_has_expected_properties` method L384-389 — `def test_service_handle_has_expected_properties(self)` — Test that FloxServiceHandle has expected properties.
- pub `test_service_handle_save_and_load` method L391-417 — `def test_service_handle_save_and_load(self)` — Test saving and loading a service handle.
- pub `test_service_handle_save_creates_file` method L419-449 — `def test_service_handle_save_creates_file(self)` — Test that save creates a JSON file.
- pub `test_service_handle_load_missing_file` method L451-454 — `def test_service_handle_load_missing_file(self)` — Test that loading a missing file raises error.
- pub `test_service_handle_repr` method L456-472 — `def test_service_handle_repr(self)` — Test that FloxServiceHandle has a repr.
- pub `TestFloxServicesProperty` class L475-489 — `{ test_services_property_returns_flox_services, test_services_property_has_same_...` — Test the Flox.services property.
- pub `test_services_property_returns_flox_services` method L478-482 — `def test_services_property_returns_flox_services(self)` — Test that flox.services returns a FloxServices instance.
- pub `test_services_property_has_same_path` method L484-489 — `def test_services_property_has_same_path(self)` — Test that services has the same path as parent Flox.
- pub `TestFloxServicesIntegration` class L496-531 — `{ flox_env, test_status_returns_list, test_services_via_flox_property }` — Integration tests for FloxServices (requires Flox CLI).
- pub `test_status_returns_list` method L516-522 — `def test_status_returns_list(self, flox_env)` — Test that status returns a list (empty if no services).
- pub `test_services_via_flox_property` method L524-531 — `def test_services_via_flox_property(self, flox_env)` — Test accessing services via Flox.services property.
- pub `TestFloxRequiredDecorator` class L534-581 — `{ test_flox_required_returns_decorator, test_flox_required_with_path_returns_dec...` — Unit tests for the flox_required decorator.
- pub `test_flox_required_returns_decorator` method L537-540 — `def test_flox_required_returns_decorator(self)` — Test that flox_required returns a decorator.
- pub `test_flox_required_with_path_returns_decorator` method L542-545 — `def test_flox_required_with_path_returns_decorator(self)` — Test that flox_required with path returns a decorator.
- pub `test_flox_required_with_services_returns_decorator` method L547-550 — `def test_flox_required_with_services_returns_decorator(self)` — Test that flox_required with services returns a decorator.
- pub `test_flox_required_decorator_wraps_function` method L552-562 — `def test_flox_required_decorator_wraps_function(self)` — Test that the decorator wraps a function.
- pub `test_flox_required_decorator_preserves_name` method L564-570 — `def test_flox_required_decorator_preserves_name(self)` — Test that decorator preserves function __name__.
- pub `test_flox_required_fails_without_flox_env` method L572-581 — `def test_flox_required_fails_without_flox_env(self)` — Test that decorated function fails without Flox environment.
- pub `TestFloxRequiredIntegration` class L588-665 — `{ flox_env, test_flox_required_activates_env, test_flox_required_deactivates_aft...` — Integration tests for flox_required decorator (requires Flox CLI).
- pub `test_flox_required_activates_env` method L608-621 — `def test_flox_required_activates_env(self, flox_env)` — Test that flox_required activates the environment.
- pub `test_flox_required_deactivates_after` method L623-635 — `def test_flox_required_deactivates_after(self, flox_env)` — Test that flox_required deactivates after function completes.
- pub `test_flox_required_passes_args_kwargs` method L637-644 — `def test_flox_required_passes_args_kwargs(self, flox_env)` — Test that flox_required passes args and kwargs correctly.
- pub `test_flox_required_exception_handling` method L646-656 — `def test_flox_required_exception_handling(self, flox_env)` — Test that flox_required deactivates even on exception.
- pub `test_flox_required_return_value` method L658-665 — `def test_flox_required_return_value(self, flox_env)` — Test that flox_required preserves return values.

#### py_tests/integrations/test_flox_extended.py

- pub `TestEnvInheritance` class L28-85 — `{ flox_env, test_activate_sets_flox_env_in_os_environ, test_deactivate_restores_...` — Bug 1: activate() must set std::env so Rust-spawned
- pub `test_activate_sets_flox_env_in_os_environ` method L46-61 — `def test_activate_sets_flox_env_in_os_environ( self, flox_env )` — After activate(), FLOX_ENV should be in os.environ.
- pub `test_deactivate_restores_environment` method L63-69 — `def test_deactivate_restores_environment(self, flox_env)` — After deactivate(), env should be restored.
- pub `test_deactivate_removes_added_keys` method L71-85 — `def test_deactivate_removes_added_keys(self, flox_env)` — Keys added during activation should be removed.
- pub `TestContextManagerServices` class L91-130 — `{ test_flox_accepts_services_parameter, test_flox_services_defaults_to_none, tes...` — Bug 2: Context manager should support services param.
- pub `test_flox_accepts_services_parameter` method L94-98 — `def test_flox_accepts_services_parameter(self)` — Flox() should accept a services parameter.
- pub `test_flox_services_defaults_to_none` method L100-103 — `def test_flox_services_defaults_to_none(self)` — Without services param, Flox still works.
- pub `test_flox_services_empty_list` method L105-108 — `def test_flox_services_empty_list(self)` — Empty services list should be accepted.
- pub `TestServicesLogsFollow` class L136-157 — `{ test_logs_follow_true_raises_value_error, test_logs_follow_false_does_not_rais...` — Bug 3: services_logs with follow=True should error.
- pub `test_logs_follow_true_raises_value_error` method L139-146 — `def test_logs_follow_true_raises_value_error(self)` — follow=True should raise ValueError.
- pub `test_logs_follow_false_does_not_raise_value_error` method L148-157 — `def test_logs_follow_false_does_not_raise_value_error( self, )` — follow=False should not raise ValueError
- pub `TestTildePathExpansion` class L163-182 — `{ test_flox_tilde_expansion, test_flox_services_tilde_expansion, test_flox_tilde...` — Bug 4: ~ paths should be expanded to home directory.
- pub `test_flox_tilde_expansion` method L166-170 — `def test_flox_tilde_expansion(self)` — Flox('~/somedir') should expand ~ to home.
- pub `test_flox_services_tilde_expansion` method L172-176 — `def test_flox_services_tilde_expansion(self)` — FloxServices('~/somedir') should expand ~.
- pub `test_flox_tilde_with_subdirectory` method L178-182 — `def test_flox_tilde_with_subdirectory(self)` — Flox('~/a/b/c') should expand correctly.
- pub `TestTimestampFormat` class L188-237 — `{ test_service_handle_timestamp_is_iso8601, test_iso8601_format_regex }` — Bug 5: chrono_now() should produce ISO 8601 timestamps.
- pub `test_service_handle_timestamp_is_iso8601` method L191-210 — `def test_service_handle_timestamp_is_iso8601(self)` — Loaded handle's started_at should be ISO 8601.
- pub `test_iso8601_format_regex` method L212-237 — `def test_iso8601_format_regex(self)` — Verify ISO 8601 format via regex on a new handle.
- pub `TestServiceHandleStopScope` class L243-273 — `{ test_handle_stop_uses_service_names }` — Bug 6: FloxServiceHandle.stop() should only stop
- pub `test_handle_stop_uses_service_names` method L247-273 — `def test_handle_stop_uses_service_names(self)` — Verify handle tracks specific service names.
- pub `TestServiceInfoConstructor` class L279-321 — `{ test_service_info_constructor, test_service_info_no_pid, test_service_info_rep...` — Bug 7: ServiceInfo should be constructable from Python.
- pub `test_service_info_constructor` method L282-287 — `def test_service_info_constructor(self)` — ServiceInfo can be created from Python.
- pub `test_service_info_no_pid` method L289-294 — `def test_service_info_no_pid(self)` — ServiceInfo can be created without pid.
- pub `test_service_info_repr_with_pid` method L296-302 — `def test_service_info_repr_with_pid(self)` — ServiceInfo repr includes pid when present.
- pub `test_service_info_repr_without_pid` method L304-309 — `def test_service_info_repr_without_pid(self)` — ServiceInfo repr omits pid when None.
- pub `test_service_info_as_tuple` method L311-315 — `def test_service_info_as_tuple(self)` — ServiceInfo.as_tuple() returns (name, status, pid).
- pub `test_service_info_as_tuple_no_pid` method L317-321 — `def test_service_info_as_tuple_no_pid(self)` — ServiceInfo.as_tuple() with no pid.
- pub `TestFloxServicesStopArgs` class L327-357 — `{ test_stop_method_is_callable, test_stop_accepts_service_names }` — FloxServices.stop() should accept service names.
- pub `test_stop_method_is_callable` method L330-334 — `def test_stop_method_is_callable(self)` — stop() should be callable.
- pub `TestFloxDecoratorProxy` class L363-408 — `{ test_decorator_preserves_name, test_decorator_preserves_doc, test_decorator_wi...` — Test flox_required decorator attribute proxying.
- pub `test_decorator_preserves_name` method L366-373 — `def test_decorator_preserves_name(self)` — Decorator preserves __name__.
- pub `test_decorator_preserves_doc` method L375-383 — `def test_decorator_preserves_doc(self)` — Decorator preserves __doc__.
- pub `test_decorator_with_services_param` method L385-395 — `def test_decorator_with_services_param(self)` — Decorator accepts services parameter.
- pub `test_decorator_fails_without_flox_env` method L397-408 — `def test_decorator_fails_without_flox_env(self)` — Decorated function fails when env doesn't exist.
- pub `TestFloxDecoratorIntegration` class L418-461 — `{ flox_env, test_decorator_activates_and_deactivates, test_decorator_exception_s...` — Integration tests for flox_required with real Flox.
- pub `test_decorator_activates_and_deactivates` method L435-449 — `def test_decorator_activates_and_deactivates( self, flox_env )` — Decorator activates env, runs func, deactivates.
- pub `test_decorator_exception_still_deactivates` method L451-461 — `def test_decorator_exception_still_deactivates( self, flox_env )` — Even on exception, decorator cleans up.
- pub `TestFloxEdgeCases` class L467-492 — `{ test_flox_path_object_input, test_flox_default_path_is_cwd, test_flox_relative...` — Edge case tests.
- pub `test_flox_path_object_input` method L470-474 — `def test_flox_path_object_input(self)` — Flox accepts pathlib.Path objects.
- pub `test_flox_default_path_is_cwd` method L476-479 — `def test_flox_default_path_is_cwd(self)` — Default path resolves to cwd.
- pub `test_flox_relative_path_resolved` method L481-484 — `def test_flox_relative_path_resolved(self)` — Relative paths are resolved to absolute.
- pub `test_flox_services_from_flox_instance` method L486-492 — `def test_flox_services_from_flox_instance(self)` — flox.services returns FloxServices with same path.

#### py_tests/integrations/test_git.py

- pub `test_git_no_working_dir` function L8-12 — `def test_git_no_working_dir()` — test git object works with no working directory specified
- pub `test_git_bad_subcommand` function L15-23 — `def test_git_bad_subcommand()` — test that object fails with bad sub-command
- pub `test_git_initialization` function L26-40 — `def test_git_initialization()` — test git object initialization

#### py_tests/integrations/test_venv.py

- pub `test_init` function L29-56 — `def test_init()` — testing creation of an environment
- pub `test_requirements_load_string` function L60-82 — `def test_requirements_load_string()` — testing load from "string"
- pub `test_requirements_load_list` function L85-105 — `def test_requirements_load_list()` — test load requirements from list
- pub `test_discover_available_pythons` function L108-122 — `def test_discover_available_pythons()` — Test UV's Python discovery functionality
- pub `test_uv_version` function L125-133 — `def test_uv_version()` — Test getting UV version
- pub `test_multiple_activations` function L300-328 — `def test_multiple_activations()` — Test that multiple activations don't cause issues
- pub `test_virtualenv_now_flag` function L331-356 — `def test_virtualenv_now_flag()` — Test that VirtualEnv with now=True actually creates the venv immediately
- pub `test_virtualenv_path_resolution` function L359-376 — `def test_virtualenv_path_resolution()` — Test absolute vs relative paths
- pub `test_virtualenv_api_availability` function L379-409 — `def test_virtualenv_api_availability()` — Test which methods and properties exist on VirtualEnv
- pub `test_cross_platform_paths` function L412-429 — `def test_cross_platform_paths()` — Test that paths work correctly on different platforms
- pub `test_error_messages` function L432-456 — `def test_error_messages()` — Test clear error messages for common issues
- pub `test_cleanup_and_state` function L459-477 — `def test_cleanup_and_state()` — Test cleanup and state management
- pub `test_install_method` function L480-502 — `def test_install_method()` — Test the install() method for installing packages
- pub `test_install_failure_scenarios` function L546-581 — `def test_install_failure_scenarios()` — Test that install fails gracefully in error scenarios
- pub `test_virtual_env_already_set` function L752-783 — `def test_virtual_env_already_set()` — Test that activation handles existing VIRTUAL_ENV correctly
- pub `TestVenvFunctionalIntegration` class L790-1114 — `{ angreal_project, test_venv_required_decorator_functional, test_venv_required_w...` — Functional tests that exercise venv through the angreal CLI.

#### py_tests/integrations/test_venv_extended.py

- pub `TestBugValidation` class L62-170 — `{ test_context_manager_installs_requirements_string, test_context_manager_instal...`
- pub `test_context_manager_installs_requirements_string` method L64-76 — `def test_context_manager_installs_requirements_string(self, venv_cleanup)` — Bug 1: context manager with requirements=string installs the package.
- pub `test_context_manager_installs_requirements_list` method L78-91 — `def test_context_manager_installs_requirements_list(self, venv_cleanup)` — Bug 1: context manager with requirements=list.
- pub `test_context_manager_installs_requirements_file` method L93-105 — `def test_context_manager_installs_requirements_file(self, venv_cleanup, tmp_path...` — Bug 1: context manager with requirements=file.txt installs from file.
- pub `test_context_manager_requirements_importable_inside` method L107-121 — `def test_context_manager_requirements_importable_inside( self, venv_cleanup )` — Bug 1: packages importable inside with block.
- pub `test_tilde_path_expansion` method L123-134 — `def test_tilde_path_expansion(self, venv_cleanup)` — Bug 3: ~/ paths expand to home directory, not literal ~.
- pub `test_tilde_path_with_subdirectory` method L136-145 — `def test_tilde_path_with_subdirectory(self, venv_cleanup)` — Bug 3: ~/sub/dir/venv expands correctly.
- pub `test_sys_exec_prefix_set_on_activation` method L147-159 — `def test_sys_exec_prefix_set_on_activation(self, venv_cleanup)` — Bug 6: sys.exec_prefix should point to venv after activation.
- pub `test_sys_exec_prefix_restored_on_deactivation` method L161-170 — `def test_sys_exec_prefix_restored_on_deactivation(self, venv_cleanup)` — Bug 6: sys.exec_prefix restored to original after deactivation.
- pub `TestNestedVenvs` class L179-302 — `{ test_nested_activation_and_deactivation, test_nested_context_managers, test_ne...`
- pub `test_nested_activation_and_deactivation` method L181-204 — `def test_nested_activation_and_deactivation(self, venv_cleanup)` — Activate A, activate B, deactivate B -> override.
- pub `test_nested_context_managers` method L206-222 — `def test_nested_context_managers(self, venv_cleanup)` — Nested with blocks both clean up properly.
- pub `test_nested_decorator_calls` method L224-247 — `def test_nested_decorator_calls(self, venv_cleanup)` — @venv_required(A) calling function with @venv_required(B) inside.
- pub `test_activate_deactivate_activate_same_venv` method L249-267 — `def test_activate_deactivate_activate_same_venv(self, venv_cleanup)` — Activate -> deactivate -> activate same venv works cleanly.
- pub `test_activate_different_venvs_sequentially` method L269-287 — `def test_activate_different_venvs_sequentially(self, venv_cleanup)` — Activate A, deactivate A, activate B, deactivate B -> clean state.
- pub `test_stacked_context_manager_with_exception_in_inner` method L289-302 — `def test_stacked_context_manager_with_exception_in_inner(self, venv_cleanup)` — Inner with raises exception, outer still deactivates cleanly.
- pub `TestContextManagerEdgeCases` class L311-397 — `{ test_context_manager_exception_still_deactivates, test_context_manager_excepti...`
- pub `test_context_manager_exception_still_deactivates` method L313-324 — `def test_context_manager_exception_still_deactivates(self, venv_cleanup)` — Exception inside with -> venv deactivated, state restored.
- pub `test_context_manager_exception_propagates` method L326-333 — `def test_context_manager_exception_propagates(self, venv_cleanup)` — Exception inside with -> exception NOT swallowed.
- pub `test_context_manager_with_now_false` method L335-345 — `def test_context_manager_with_now_false(self, venv_cleanup)` — with VirtualEnv(path, now=False) creates venv on enter.
- pub `test_context_manager_returns_venv_instance` method L347-355 — `def test_context_manager_returns_venv_instance(self, venv_cleanup)` — with VirtualEnv(...) as venv -> venv is the VirtualEnv instance.
- pub `test_context_manager_subprocess_inherits_env` method L357-368 — `def test_context_manager_subprocess_inherits_env(self, venv_cleanup)` — subprocess.run inside with block uses venv python.
- pub `test_context_manager_no_requirements` method L370-380 — `def test_context_manager_no_requirements(self, venv_cleanup)` — with VirtualEnv(path) works fine with no requirements.
- pub `test_context_manager_reentry` method L382-397 — `def test_context_manager_reentry(self, venv_cleanup)` — Entering same context manager instance twice works or raises safely.
- pub `TestDecoratorEdgeCases` class L405-509 — `{ test_decorator_preserves_function_name, test_decorator_preserves_docstring, te...`
- pub `test_decorator_preserves_function_name` method L407-416 — `def test_decorator_preserves_function_name(self, venv_cleanup)` — Decorated function's __name__ matches original.
- pub `test_decorator_preserves_docstring` method L418-428 — `def test_decorator_preserves_docstring(self, venv_cleanup)` — Decorated function's __doc__ matches original.
- pub `test_decorator_preserves_arguments_attribute` method L430-441 — `def test_decorator_preserves_arguments_attribute(self, venv_cleanup)` — Decorated function proxies __arguments for angreal introspection.
- pub `test_decorator_getattr_proxy` method L499-509 — `def test_decorator_getattr_proxy(self, venv_cleanup)` — Decorated function proxies custom attributes from original.
- pub `TestPathHandling` class L517-581 — `{ test_path_with_spaces, test_path_with_unicode, test_path_object_input, test_pa...`
- pub `test_path_with_spaces` method L519-526 — `def test_path_with_spaces(self, venv_cleanup, tmp_path)` — VirtualEnv with spaces in path works.
- pub `test_path_with_unicode` method L528-535 — `def test_path_with_unicode(self, venv_cleanup, tmp_path)` — VirtualEnv with unicode in path works.
- pub `test_path_object_input` method L537-544 — `def test_path_object_input(self, venv_cleanup, tmp_path)` — VirtualEnv(path=Path("my-venv")) works.
- pub `test_path_resolution_with_symlinks` method L546-564 — `def test_path_resolution_with_symlinks(self, venv_cleanup, tmp_path)` — Path through symlink resolves correctly.
- pub `test_relative_path_anchored_to_cwd` method L566-573 — `def test_relative_path_anchored_to_cwd(self, venv_cleanup)` — Relative path is resolved from cwd at creation time.
- pub `test_absolute_path_unchanged` method L575-581 — `def test_absolute_path_unchanged(self, venv_cleanup, tmp_path)` — Absolute path is not modified by resolution (other than resolve()).
- pub `TestInstallStress` class L589-684 — `{ test_install_empty_list, test_install_nonexistent_package, test_install_nonexi...`
- pub `test_install_empty_list` method L591-598 — `def test_install_empty_list(self, venv_cleanup)` — venv.install([]) is a no-op or handles cleanly.
- pub `test_install_nonexistent_package` method L600-607 — `def test_install_nonexistent_package(self, venv_cleanup)` — venv.install("nonexistent-pkg-xyz-999") raises RuntimeError.
- pub `test_install_nonexistent_requirements_file` method L609-616 — `def test_install_nonexistent_requirements_file(self, venv_cleanup)` — venv.install("nonexistent.txt") raises RuntimeError.
- pub `test_install_version_pinned_package` method L618-631 — `def test_install_version_pinned_package(self, venv_cleanup)` — venv.install("six==1.16.0") installs exact version.
- pub `test_install_path_object` method L655-670 — `def test_install_path_object(self, venv_cleanup, tmp_path)` — venv.install(Path("requirements.txt")) works.
- pub `test_install_requirements_method_vs_install` method L672-684 — `def test_install_requirements_method_vs_install(self, venv_cleanup)` — install_requirements() and install() both work for packages.
- pub `TestStateAndProperties` class L692-738 — `{ test_name_property_value, test_exists_property_false_before_create, test_exist...`
- pub `test_name_property_value` method L694-700 — `def test_name_property_value(self, venv_cleanup)` — venv.name reflects the input path string.
- pub `test_exists_property_false_before_create` method L702-705 — `def test_exists_property_false_before_create(self)` — now=False -> venv.exists == False.
- pub `test_exists_property_true_after_create` method L707-713 — `def test_exists_property_true_after_create(self, venv_cleanup)` — now=True -> venv.exists == True.
- pub `test_exists_property_false_after_remove` method L715-723 — `def test_exists_property_false_after_remove(self, venv_cleanup)` — After venv.remove() -> venv.exists == False.
- pub `TestRemoveMethod` class L746-794 — `{ test_remove_deletes_directory, test_remove_on_nonexistent_is_safe, test_remove...`
- pub `test_remove_deletes_directory` method L748-757 — `def test_remove_deletes_directory(self, venv_cleanup)` — venv.remove() deletes the venv directory.
- pub `test_remove_on_nonexistent_is_safe` method L759-763 — `def test_remove_on_nonexistent_is_safe(self)` — venv.remove() on already-removed/never-created venv doesn't crash.
- pub `test_create_after_remove` method L783-794 — `def test_create_after_remove(self, venv_cleanup)` — remove() then create() works cleanly.
- pub `TestSysPathIntegrity` class L803-882 — `{ test_sys_path_contains_site_packages_after_activation, test_sys_path_order_ven...`
- pub `test_sys_path_contains_site_packages_after_activation` method L805-819 — `def test_sys_path_contains_site_packages_after_activation(self, venv_cleanup)` — Venv site-packages in sys.path after activation.
- pub `test_sys_path_order_venv_first` method L821-845 — `def test_sys_path_order_venv_first(self, venv_cleanup)` — Venv site-packages appears before system site-packages in sys.path.
- pub `test_sys_path_fully_restored_after_deactivation` method L847-859 — `def test_sys_path_fully_restored_after_deactivation(self, venv_cleanup)` — Exact sys.path list restored after deactivation.
- pub `test_sys_path_no_duplicates_after_reactivation` method L861-882 — `def test_sys_path_no_duplicates_after_reactivation(self, venv_cleanup)` — activate/deactivate/activate doesn't duplicate sys.path entries.
-  `_save_state` function L36-44 — `def _save_state()` — Capture current Python/env state for later comparison.
-  `_assert_state_restored` function L47-53 — `def _assert_state_restored(original)` — Assert that Python/env state matches the saved snapshot.
