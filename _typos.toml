# Spell-check configuration (crate-ci/typos).
# docs/api is generated by Plissken from Rust/PyO3 source and is not authored
# prose, so it is excluded from spell checking.
[files]
extend-exclude = ["docs/api"]
